# 30. Meta Andromeda 評分模型真實準確率分析與改善建議

- 建立日期:2026-07-13
- 狀態:待議(分析報告,尚未立案實作)
- 關聯文件:docs/19(閉環優化建議)、docs/20(P0-6 release 機制與示範資料標示)、docs/22、docs/28

---

## 1. 背景

docs/20 P0-6 交付 release 工作台時,版本總覽上的「成對排序準確率/平均級距誤差」為 seed 示範值,並以 `is_demo_data` 旗標 + 警告橫幅標示,避免營運誤讀。真實數字需呼叫 `POST /release/{model_version}/refresh-metrics`,從 drift report 實測配對結果計算後寫回(`metrics_source: seed → computed`)。

2026-07-13 首次對現任 production 版本執行 refresh-metrics,真實指標首度曝光,與示範值差距顯著,且方向上與版本總覽「線上實測對照證據」區塊的嚴重預估偏差警示一致。本文件記錄現況數據、原因分析與改善建議。

## 2. 現況數據(2026-07-13)

### 2.1 真實指標 vs 示範值

| 指標 | 示範值(seed) | 真實值(computed) | 解讀 |
|---|---|---|---|
| 成對排序準確率 pairwise_ranking_accuracy | 0.78 | **0.4446** | 低於 0.5(隨機猜測),模型對「哪個素材成效較好」的排序判斷**略差於丟銅板** |
| 平均級距誤差 mean_band_error | 0.17 | **0.8218** | ROAS 級距(low/mid/high 三級)預測平均偏差近一整級 |

- 計算樣本:331 筆 drift-matched 配對(觀測素材 × 對應 AI 評分事件)
- 計算範圍:僅計入 `lineage.registry_model_version == cand_v2026_06_05_a`、`scoring_mode == ai`、有花費且 ROAS 預測 eligible 的配對

### 2.2 佐證:線上實測對照(drift report,最近 7 天)

- 排名相關係數 ρ = **-0.461**(負相關:模型給高分的素材,實際成效反而偏差)
- 預測準確率 = **31.0%**
- 報告狀態:嚴重預估偏差

兩組數字來自同一批配對資料的不同視角,互相印證:**現任評分模型對實際成效的預測力很弱,排序方向甚至偏反**。

### 2.3 運行環境現況

| 項目 | 數值 |
|---|---|
| 現任 production 版本 | cand_v2026_06_05_a(feature manifest:fm_cand_20260605_a) |
| 實際評分模型 | openrouter `nvidia/nemotron-3-nano-omni-30b-a3b-reasoning:free`(免費 nano 級模型) |
| 評分事件 | 累計 761 筆,completed 628 筆,**全部**由上述同一模型產生 |
| 觀測素材 | 355 筆(全部有花費) |
| 前一版 prod_v2026_05_28 | 無任何評分事件掛在此版本(insufficient_data),指標維持 seed,無法作為對照組 |

## 3. 原因分析(假設,按可能性排序)

1. **模型能力不足(最可能)**:現用 nemotron-3-nano 為免費 nano 級全模態模型,先前為排除 400 context 錯誤與成本考量而選用。素材成效判斷需要細緻的視覺+行銷語境理解,nano 級模型可能只是「看圖說故事」,給分與實際成效脫鉤。628 筆評分全出自同一模型,無對照組可排除此因素。
2. **評分 rubric / prompt 與標籤定義錯位**:模型評的是「素材品質」,但 drift 配對比的是「實際 ROAS 級距」。素材美感與投放成效本來就非強相關(受眾、出價、產品價格帶都會影響 ROAS),rubric 若未針對「預測成效」設計,準確率低是結構性的。
3. **標籤/門檻策略問題**:`label_observed_band` 的門檻由觀測資料動態計算(compute_label_thresholds),355 筆樣本橫跨不同 objective/市場,若分布傾斜,級距切分本身可能不穩定。
4. **觀測窗口不一致**:評分在素材上線初期,觀測成效在 last_7d/last_30d/lifetime 不同窗口,同一素材不同窗口的 ROAS 級距可能不同,配對時引入雜訊。
5. **負相關(ρ=-0.461)的特別訊號**:若純粹是模型無能,相關係數應趨近 0 而非顯著為負。顯著負相關可能暗示系統性偏差 — 例如模型偏好「精緻、資訊量大」的素材,而實際上粗糙直接的素材在該帳戶跑得更好。這一點值得人工抽樣 20-30 筆高分低效/低分高效案例驗證。

## 4. 改善建議

### 4.1 短期(不改架構)

1. **人工抽樣歸因**:從 331 筆配對中抽「模型高分×實際低效」與「模型低分×實際高效」各 15 筆,人工看素材,判斷是模型亂評(假設 1)還是評分維度與成效本質脫鉤(假設 2)。此步驟決定後續投資方向,成本最低,建議最先做。
2. **換模型回測**:用監控總覽的候選模型驗證功能挑 2-3 個更強的多模態模型(需支援 image input、context ≥ 32k),以現有 355 筆觀測素材 + 校準資料集回測(backtest purpose 已支援 env 覆寫指定模型),比較 pairwise_ranking_accuracy 是否顯著超過 0.5。免費 nano 模型與付費中階模型的差距是第一個要驗證的變因。
3. **refresh-metrics 例行化**:目前需手動呼叫。建議每次 drift report 產生後自動 refresh 現任版本指標,讓版本總覽常態顯示真實數字(小改動:掛在既有 drift 排程尾端)。

### 4.2 中期(需立案)

4. **rubric 重設計**:若抽樣證實假設 2,評分 prompt 應從「素材品質評分」轉向「同帳戶歷史高效素材的特徵對齊度」,並將 objective/受眾/價格帶納入 context。
5. **級距預測與排序分離**:mean_band_error 0.82 顯示三級制 ROAS 預測基本不可用。可考慮:評分只輸出相對排序(比較同 batch 素材),級距標籤改由歷史統計模型(非 LLM)產生。
6. **上線門檻**:核准流程加硬性門檻 — 候選版本回測 pairwise_ranking_accuracy < 0.55 不得核准上線,避免再次出現「上線後才發現不如隨機」。

## 5. 驗收條件(若立案)

- [ ] 完成高分低效/低分高效人工抽樣歸因,產出結論(假設 1 vs 2)
- [ ] 至少 2 個候選模型完成回測,有 pairwise_ranking_accuracy 對照表
- [ ] refresh-metrics 自動化,版本總覽指標與 drift report 同步更新
- [ ] 新候選版本上線前通過 ≥ 0.55 排序準確率門檻

## 6. 附註

- 本次 refresh-metrics 執行紀錄:2026-07-13,`cand_v2026_06_05_a` → status computed、sample_count 331;`prod_v2026_05_28` → insufficient_data(無掛載評分事件)。
- 版本總覽的示範資料警告橫幅已隨 `is_demo_data=false` 消失;前一版卡片數字(0.74/0.19)仍為 seed 值,因無資料可算,閱讀時注意。
