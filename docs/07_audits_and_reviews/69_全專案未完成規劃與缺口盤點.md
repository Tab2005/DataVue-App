# 69. 全專案未完成規劃與缺口盤點

## 基本資訊

- 盤點日期：2026-08-07
- 盤點範圍：`docs/` 全目錄（01_system、02_ga4、03_meta_andromeda、04_mmm、05_gsc、06_features_and_ui、07_audits_and_reviews）
- 盤點方式：逐份閱讀文件內容找「待做/選配/待議/明確不做」等狀態標記，並對關鍵claim 做程式碼交叉驗證（grep 確認功能是否真的存在）；只列出目前仍懸而未決、且沒有找到後續完成證據的項目。
- 性質：**純盤點清單，不含優先順序建議**，是否處理、何時處理由後續另行決定。

---

## 01_system

### Roadmap 未勾選項目
- 文件：`docs/01_system/01_專案概覽.md`
- 缺口：「跨平台數據自動對比功能」「戰略警示系統」兩項仍是計畫中、未勾選；「GA4 電商與行為分析擴充」標記進行中，實際完成度未知。
- 狀態用語：文件自身標「計划中」。
- 信心：文件明確勾選標記。

### 背景 Worker 架構統一 — 核心機制未落地
- 文件：`docs/01_system/25_背景工作負載統一Worker架構規劃.md`
- 缺口：整案「待議」。且 Wave 1 的核心動作（`_dispatch_analysis` 依 `SERVICE_ROLE` 分流、web 角色回 `db_queue`、`sweep_contribution_queue()` 撿件送分析）在程式碼中完全不存在（`db_queue`/`sweep_contribution_queue` 全域搜尋零匹配）；目前只有殭屍回收（`sweep_contribution_stale_snapshots`，源自 docs/27）在跑。Wave 2（GA4 job 掛 worker）已生效。Wave 3（服務更名/記憶體上限）明確「選配，低優先」未做。
- 狀態用語：待議。
- 信心：程式碼驗證。

### Meta Andromeda 獨立 Worker 文件用語落後現況
- 文件：`docs/01_system/10_Zeabur_部署與持久化儲存設定指南.md` §2.4
- 缺口：標「選配」，但生產環境實際已走 Option D 集中化儲存 worker，應已是現況而非選配，文件用語與現況有落差。
- 信心：與既有記憶／部署現況比對推論。

---

## 02_ga4

### 查詢並行化未擴及全部端點
- 文件：`docs/02_ga4/65_GA4查詢並行化實作規劃.md`
- 缺口：`kpi.py`、`anomaly_rules.py` 仍是迴圈式查詢、未並行化，文件明講「另案處理，日後可直接照抄本次做法」。
- 信心：明確文字。

### 分享連結：到期時間與存取紀錄
- 文件：`docs/02_ga4/59_GA4分享連結凍結副本實作規劃.md`、`63_GA4轉換洞察渠道標籤精準化實作計劃.md`（撤銷功能）
- 缺口：`expires_at`（到期時間）與存取紀錄兩項，原文「可視需求排期」，撤銷功能已做但這兩項不在範圍內，至今無後續文件處理。
- 狀態用語：待議/選配。
- 信心：明確文字。

### 跨 property 批次盤點歸因模式
- 文件：`docs/02_ga4/34_GA4_轉換洞察渠道標籤精準化實作計劃.md`
- 缺口：列在「明確不做」，備註「需要時可另案追加」，目前無此追加案。
- 信心：明確文字。

### 商品×到達頁交叉分析延伸功能
- 文件：`docs/02_ga4/47`（商品×到達頁交叉，文件標題以此為準）
- 缺口：「商品拖累頁面」反向差異標記、多到達頁完整清單呈現、交叉頁的渠道篩選，三項皆明確排除本次範圍，無後續案。
- 信心：明確文字。

### 當日總覽轉換事件拆分——驗證步驟與範圍缺口
- 文件：`docs/02_ga4/50`（當日總覽轉換事件拆分，文件標題以此為準）
- 缺口：步驟 4「GA4 API 相容性人工驗證」未像步驟 1-3 標記已完成；「渠道對照」分頁的轉換指標拆分明確排除在外，無後續文件跟進。
- 信心：文件狀態欄位缺漏 + 明確排除文字。

### 週報功能文件未回填完成狀態（非真缺口，僅記錄避免誤判）
- 文件：`docs/02_ga4/09_GA4週報功能實作計畫.md`
- 現況：文件任務清單（Phase 1-4）全部 `[ ]` 未勾選，但程式碼確認（`report_service.py`、`routers/reports.py`、scheduler、`ReportGA4PropertySelector.jsx`）功能已完整實作。**這不是缺口，純粹是文件從未回填完成狀態**，特別記錄以免日後被誤判為待辦。
- 信心：程式碼驗證。

---

## 04_mmm

### GA4 雙 y 對照整合（第三波 A）——完全未開始
- 文件：`docs/04_mmm/21`（MMM 貢獻分析，文件標題以此為準）第三波 A
- 缺口：標「建議實作」，內容是 Meta-y / GA4-y 雙模型對照（歸因通膨指數）+ GA4 控制變數。`backend/modules/contribution/` 全目錄搜尋 `ga4`/`GA4` 字樣零匹配，確認完全未動工。
- 信心：明確文字 + 程式碼驗證。

### 自動化與整合（第三波 B）——四項全部未做
- 文件：同上，第三波 B
- 缺口：明確標「選配，依使用回饋決定」。含週期自動更新、快照對比視圖、與 Andromeda 資料整合、跨渠道 MMM 擴充，四項全部未做。`docs/26_MMM_貢獻分析結果判讀指南.md` §8.5 也自行寫著「與 GA4 雙 y 對照（尚未上線）」與「預算最佳化任務（尚未立案）」，互相印證。
- 信心：明確文字 + 交叉印證。

### 驗收待補
- 文件：`docs/04_mmm/27`（MMM 分組追加優化，文件標題以此為準）任務 3.1
- 缺口：驗收標準「對真實帳戶跑一次 auto_group 人工核對」明確標「未完成項」，因開發環境無真實帳號可測，待上線後補做。
- 信心：明確文字。

（docs/27 本身另有三項「明確不修」的刻意決策——Hill K 選模 leakage、前後端時區微差、GroupEditor 新增組別功能——維持不動，非缺口。）

---

## 05_gsc

### GSC API 資料呈現擴充第 2-7 項
- 文件：`docs/05_gsc/29`（GSC API 資料呈現擴充，文件標題以此為準）
- 缺口：文件頭部自陳狀態——第 1 項（搜尋外觀）已透過 docs/35 完成，**第 2-7 項「仍待實作」**：Search Type 成效、Sitemap 健康狀態、URL Inspection 頁面診斷。
- 信心：文件明確狀態欄位。

### 搜尋類型/裝置/國家交叉分析——規劃中未實作
- 文件：`docs/05_gsc/36`（文件標題以此為準）
- 缺口：整份文件狀態「規劃中，尚未實作」，是 docs/29 第 2-4 項的落地規劃。`backend` 搜尋 `search_type` 參數零匹配，確認尚未動工，無任何後續完成紀錄文件。
- 信心：文件狀態欄位 + 程式碼驗證。

### 內容缺口 AI 建議——端到端驗證與做法 B
- 文件：`docs/05_gsc/37`（文件標題以此為準）
- 缺口：Phase 1、2 都完成，但兩處都明確寫「未做：實際瀏覽器 / 真實 API Key 端到端驗證」（本機無真實帳號可測）。「做法 B：全站規模機會關鍵字→全新文章題目」列為「本階段不做」，結尾重申「待做法 A 上線並取得使用者實際使用回饋後，再評估是否啟動」，目前無任何跟進。
- 信心：明確文字。

---

## 06_features_and_ui

### Landing Page 改版文件與現況嚴重不符
- 文件：`docs/06_features_and_ui/11`（Landing Page 改版，文件標題以此為準）
- 缺口：文件狀態欄寫「已完成實作」，Phase 1-3 全部打勾，12 個新/改檔案列表全部標記完成。但實際檢查程式碼，`frontend/src/components/Landing/` 整個目錄不存在，`LogoBar.jsx`/`SocialProof.jsx`/`FinalCTA.jsx`/`MetaAndromeda.jsx` 等文件描述的元件全部沒有。git log 顯示後續有 commit（`78d2ac6`、`2e8d1b3`）把這次改版整個換掉，改成現在單檔 141 行的極簡版 `Landing.jsx`（Hero + 核心模組 + 運作流程三段），但文件從未更新反映這次替換。文件「七、後續追蹤」留的三個未解問題（真實用戶評語、LogoBar 動態數字接 API、Meta Andromeda CTA 導向）也已隨改版被替換而失去意義。
- 信心：程式碼與 git log 直接驗證，是本次盤點發現的最大一處文件/現況落差，建議之後找時間標記過時或改寫，避免誤導。

### 快照/分享清單頁未跟進
- 文件：`docs/06_features_and_ui/58`（成效分析頁 AI 廣告分析改版，文件標題以此為準）
- 缺口：「不在本次範圍內」列了快照/分享清單頁（使用者無法看到自己產生過哪些快照，只能靠分享連結存取），至今無後續文件跟進。
- 信心：明確文字。

### 週報功能文件未回填完成狀態（非真缺口，僅記錄避免誤判）
- 文件：`docs/06_features_and_ui/08_週報功能實作方案.md`
- 現況：與 docs/09 同款情況——任務清單全部未勾選，但功能（`WeeklyReport`/`ReportSchedule`/`share_token`/`ReportConfig.jsx` 精靈）程式碼確認已完整實作，包含後續構想欄位提到的「分享連結」「定時自動週報」也都已支援。純粹文件未回填。
- 信心：程式碼驗證。

---

## 07_audits_and_reviews

### 大型檔案拆分重構——兩個未解決策點（2026-08-10 已裁定並執行完畢）
- 文件：`docs/07_audits_and_reviews/33_大型檔案拆分重構實作計劃.md`
- 原缺口：
  1. 「前端頁面組合層 <400 行」是否仍作硬性標準——多個頁面（`Analytics.jsx`、`ContributionAnalysis.jsx`、`GA4Insights.jsx` 等）目前都超標，文件說需要確認是否調整標準。
  2. `from ._shared import *` 模式是否要全面替換為 `_shared.NAME` 存取——目前只修了一個因此爆掉的真實 bug（`SessionLocal` monkeypatch 打不到子模組），其餘子模組是否有同類風險未做全面盤點。
  - 「5 個 bonus 修復」文件寫「建議之後有空時確認是哪個拆分動作的副作用」，至今未見確認記錄。
- 處理：
  1. **維持 <400 行標準，已執行完畢**——`Analytics.jsx`（946→246 行）、`ContributionAnalysis.jsx`（674→292 行）、`GA4Insights.jsx`（634→391 行）皆已拆到位，詳見 docs/33「決策點」段落。
  2. **裁定全面替換，已執行完畢**——`backend/modules/meta_andromeda/` 下 22 個檔案（`router/`/`service/`/`repository/`）的 `from .X import *` 全數改為明確的 `_shared.NAME` 存取，過程中額外發現並修正兩類 ruff 無法正確判斷的例外（`__all__` 字串再匯出、執行期注入模組全域命名空間的名稱），以及 `service/__init__.py` 套件門面遺漏的外部可見屬性（`queue_host_adapter`）；全套件 598 tests 通過，詳見 docs/33「決策點」段落。
  - 「5 個 bonus 修復」的確認記錄仍未見，未在本次處理範圍內。
- 信心：文件明確文字 + 程式碼驗證。

### 第 7 波狀態與 docs/31 互相矛盾（2026-08-10 已修復）
- 文件：`docs/07_audits_and_reviews/33` 第 7 波（`GSCStats.jsx`/`GA4Stats.jsx`/`ga4/insights_service.py` 拆分）vs `docs/01_system/31`
- 缺口：docs/33 標「未啟動，維持需求驅動」，但 docs/31 後續更新記錄顯示這三個檔案實際上已在 2026-07-16/17 被拆分完成——兩份文件狀態互相矛盾，docs/33 沒有回填。
- 處理：已核對程式碼現況（`GSCStats.jsx` 232 行、`GA4Stats.jsx` 302 行、`ga4/insights_service.py` 5 行，皆已拆分完成），回填 docs/33 頂部狀態列與「九、第 7 波」段落，並更新「十、建議執行順序」。純文件修正，無程式碼變動。
- 信心：文件間比對 + 程式碼驗證。

### CODE_REVIEW_ACTION_PLAN 待處理項
- 文件：`docs/07_audits_and_reviews/CODE_REVIEW_ACTION_PLAN_2026-07-01.md`
- 缺口：
  - **P1-1**（2026-08-10 已完成）：`ga4`/`gsc`/`ai_hub` 空殼模組去留決策——`backend/routers/ga4.py`/`gsc.py`/`ai.py`（根層）與 `backend/modules/ga4/`、`modules/gsc/`、`modules/ai_hub/` 兩套並存的問題，其實分兩層：router 層（三個 `modules/X/router.py` 皆為轉發根層的 shim）已隨 P1-2 解決；**service 層**（`modules/gsc/service.py`、`modules/ai_hub/service.py` 原本轉發根目錄 `gsc_service.py`/`ai_service.py` 的空殼；ga4 的 service 層已在更早的 docs/22 完成真遷移）**已於本次補完**——真實實作搬入 `modules/gsc/service.py`／`modules/ai_hub/service.py`，根目錄兩檔改為薄轉發層（比照 `ga4_service.py` 既有寫法）；順帶修正兩個模組 `__init__.py` 的 eager import 循環匯入風險（改為 lazy，比照 `modules/ga4/__init__.py`）。全套件 598 tests 通過。`modules/ga4/README.md` 內容過時、未反映實際已擴充的 `insights/` 子模組，此項仍待處理（與 P1-1 主線無關，未在本次範圍內）。
  - **P1-2**（2026-08-10 已完成）：路由統一到 `modules/*/router.py`——`backend/routers/` 下 13 個路由檔全數搬進對應的 `modules/*/router.py`（含新建 `modules/users/`、`modules/teams/`、`modules/permissions/`、`modules/reports/`、`modules/saved_views/`、`modules/line/`、`modules/admin/` 七個模組），逐檔核對 OpenAPI 路徑與全套件測試；過程中發現並回收 `modules/auth/router.py` 一份分歧的死碼（提供已被其他路徑取代的 `/me`、`/ai-settings`），詳見該文件 P1-2 段落。
  - **P1-3**（2026-08-10 已完成）：`config.py` 遷移至 Pydantic `BaseSettings`，改採扁平設計（非原規劃的巢狀子模型），保留全部既有 `settings.META_ANDROMEDA_*` 呼叫點不變；過程中發現並修復 89 處測試依賴「即時讀 env」的舊行為，詳見該文件 P1-3 段落與新增的 `tests/test_config.py`（11 測試）。
  - **P2-4**（2026-08-10 已盤點，暫緩執行）：指標定義單一事實來源。查證發現這其實是「先前已做到一半但沒接上」的狀態，非從零開始：後端 `GET /api/metrics/registry` 端點與前端 `useMetricsRegistry.js`（含 React Query 快取＋備援清單）都已經蓋好，但**從未被任何元件呼叫過**；後端的 registry 只有 ~30 個指標，前端實際用的 `constants/metricsRegistry.js` 有 ~60 個。真要接上需要（a）後端補齊 registry 到 ~60 個（手工比對轉錄有出錯風險）、（b）前端把目前模組載入時同步算好的 `ALL_METRIC_GROUPS`（`analyticsMetrics.js`）改寫成用 `useMetricsRegistry()` 的 React hook（同步改非同步的架構調整，牽動本次 session 稍早才拆分完成的 `Analytics.jsx`），實際工作量比原規劃「1 天、風險低」大得多，詳見該文件 P2-4 段落的完整盤點記錄。
  - **P2-2**（前端漸進導入 TypeScript）、**P2-3**（測試覆蓋補強，如 hook 單元測試——`core/config` 單元測試已隨 P1-3 補上）——皆列「待處理」，未見後續完成文件。
  - 附帶發現的小案：`exceptions.py` 重複、`ai_hub` 與 `services/ai` 語意重複——文件說「建議另立清理案，非本計劃範圍」，目前無此清理案。（原本一併列出的「根目錄 `ga4_service.py`/`gsc_service.py`/`ai_service.py` 與 `modules/` 版本並存」已隨 P1-1 於 2026-08-10 完成真遷移，三者現在都是刻意保留的薄轉發層，非待清理項。）
  - **P2-1**（2026-08-10 回填確認為已完成）：拆分巨型檔案。原本只註記「已被 docs/33 大量處理，不算純粹遺留」，本次逐一核對現況確認範圍已全部涵蓋：`repository.py`／`service.py`／`core/scheduler.py` 皆已拆成套件、根目錄同名巨檔已不存在；`MetaAndromedaMonitoring.jsx` 降至 20 行；`Analytics.jsx`/`ContributionAnalysis.jsx`/`GA4Insights.jsx` 三頁已補拆至 <400 行。已回填 `CODE_REVIEW_ACTION_PLAN_2026-07-01.md` 的 P2-1 節與執行順序總表狀態列，兩份文件從此對 P2-1 的認定一致。已知的限定範圍例外（`SettingsModal.jsx`、`ContributionAnalysisComponents.jsx` 未拆到標準行數）皆為 docs/33 自行裁定的觀察項，非遺漏。
- 信心：文件明確列表 + 程式碼驗證。

### 過時歷史快照（不列為缺口，僅記錄排除原因）
- 文件：`archive/CODE_AUDIT_2026-02-23.md`、`archive/CODE_REVIEW_2026-01-16.md`、`archive/CODE_REVIEW_2026-02-23_by_Sonnet4.6.md`、`archive/OPTIMIZATION_REPORT.md`（2026-08-07 已搬入 `docs/07_audits_and_reviews/archive/` 子目錄整理）
- 說明：抽查確認為明顯過時的歷史快照（提到的 `backend/database.py` 單檔、`backend/debug_start.py` 等檔案現已不存在，架構早已模組化），README 也歸類為「重構歷史」而非現行待辦。未逐項列入缺口，避免把已被架構重寫掩蓋的舊發現誤判為現存問題。

---

## 03_meta_andromeda

本次盤點對此目錄只做快速掃描（已有 docs/68 專項審查覆蓋大部分現況），確認除 docs/68 外沒有其他新缺口。docs/68 本身列出的 A1-B7 全部項目已於 2026-08-07 實作完成並合併（A8、B7 依文件建議維持不做，B2 僅完成前端併發化的第一層，後端批次端點未做），詳見 docs/68 文件開頭新增的完成狀態表。
