# C-1：建立 pytest 測試框架

> **優先級**：🔴 Critical  
> **預計工時**：4–8 小時  
> **執行時程**：部署前立即完成  
> **審查問題編號**：C-1

---

## 問題說明

`backend/tests/` 目錄與 `backend/pytest.ini` 完全不存在。  
FINAL_OPTIMIZATION_REPORT.md 宣稱已建立 26 個測試案例，但實際磁碟上完全不存在。

```powershell
Test-Path "backend/tests"     # → False
Test-Path "backend/pytest.ini" # → False
```

**風險**：
- 優化報告的驗收狀態具有誤導性
- 任何重構均無安全網
- CI/CD 執行 `pytest tests/` 直接失敗

---

## 實作步驟

### Step 1：建立目錄結構

```
backend/
├── tests/
│   ├── __init__.py
│   ├── conftest.py          ← 共用 fixtures
│   ├── test_health.py       ← 健康端點測試
│   ├── test_auth.py         ← 認證流程測試
│   ├── test_permissions.py  ← 權限模組測試
│   ├── test_cache.py        ← 快取邏輯測試
│   ├── test_users.py        ← 使用者 CRUD 測試
│   └── test_integration_service.py  ← UserIntegration 測試
└── pytest.ini
```

```powershell
# 建立目錄
New-Item -ItemType Directory -Path "backend/tests" -Force
New-Item -ItemType File -Path "backend/tests/__init__.py" -Force
```

---

### Step 2：建立 pytest.ini

**檔案**：`backend/pytest.ini`

```ini
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    unit: 單元測試（不需要 DB 連線）
    integration: 整合測試（需要 DB 連線）
    slow: 執行時間較長的測試
asyncio_mode = auto
```

---

### Step 3：建立 conftest.py（共用 Fixtures）

**檔案**：`backend/tests/conftest.py`

```python
"""
共用測試 Fixtures

使用 SQLite in-memory 作為測試資料庫，確保：
1. 每個測試函式使用獨立的 DB transaction（測試後自動 rollback）
2. 不干擾本地開發的 SQLite 檔案
3. 不需要 PostgreSQL 連線即可執行所有單元測試
"""
import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from unittest.mock import patch, MagicMock

from database.base import Base
from database import get_db
from main import app

# ─── 測試資料庫設定 ────────────────────────────────────────────────
SQLALCHEMY_TEST_URL = "sqlite:///:memory:"

engine = create_engine(
    SQLALCHEMY_TEST_URL,
    connect_args={"check_same_thread": False},
    # 輸出 SQL 語法便於 debug（可改 False 關閉）
    echo=False,
)
TestingSessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)


# ─── Session-level Fixtures ───────────────────────────────────────
@pytest.fixture(scope="session", autouse=True)
def create_test_tables():
    """建立所有資料表（整個測試 session 共用）"""
    Base.metadata.create_all(bind=engine)
    yield
    Base.metadata.drop_all(bind=engine)


# ─── Function-level Fixtures ──────────────────────────────────────
@pytest.fixture
def db():
    """
    提供一個在測試後自動 rollback 的 DB session。
    保證每個測試函式之間互相隔離。
    """
    connection = engine.connect()
    transaction = connection.begin()
    session = TestingSessionLocal(bind=connection)

    yield session

    session.close()
    transaction.rollback()
    connection.close()


@pytest.fixture
def client(db):
    """
    提供 FastAPI TestClient，並覆寫 get_db 依賴。
    """
    def override_get_db():
        try:
            yield db
        finally:
            pass  # rollback 由 db fixture 負責

    app.dependency_overrides[get_db] = override_get_db
    with TestClient(app) as c:
        yield c
    app.dependency_overrides.clear()


@pytest.fixture
def mock_verify_google_token():
    """
    Mock Google Token 驗證，避免實際呼叫 Google API。
    回傳一個標準的已驗證 payload。
    """
    mock_payload = {
        "sub": "google_test_user_123",
        "email": "test@example.com",
        "email_verified": True,
        "name": "Test User",
        "picture": "https://example.com/photo.jpg",
    }
    with patch("core.security.verify_google_token", return_value=mock_payload):
        yield mock_payload


@pytest.fixture
def mock_redis():
    """Mock Redis 連線，讓快取測試不依賴真實 Redis 服務"""
    with patch("redis_cache.get_redis_client") as mock:
        mock_client = MagicMock()
        mock_client.get.return_value = None  # 預設快取 miss
        mock_client.set.return_value = True
        mock_client.delete.return_value = 1
        mock.return_value = mock_client
        yield mock_client


# ─── 測試資料 Factories ──────────────────────────────────────────
@pytest.fixture
def sample_user(db):
    """建立一個測試用 User 實例"""
    from database.models.user import User, UserRole, UserStatus

    user = User(
        google_id="google_test_user_123",
        email="test@example.com",
        name="Test User",
        role=UserRole.USER,
        status=UserStatus.ACTIVE,
    )
    db.add(user)
    db.commit()
    db.refresh(user)
    return user


@pytest.fixture
def sample_admin_user(db):
    """建立一個測試用管理員 User 實例"""
    from database.models.user import User, UserRole, UserStatus

    user = User(
        google_id="google_admin_user_456",
        email="admin@example.com",
        name="Admin User",
        role=UserRole.SUPER_ADMIN,
        status=UserStatus.ACTIVE,
    )
    db.add(user)
    db.commit()
    db.refresh(user)
    return user
```

---

### Step 4：建立測試檔案

#### 4-1：`tests/test_health.py` — 健康端點

```python
"""
/health 和 /api/health 端點測試
屬於最基礎的 smoke test，確認服務可正常啟動與回應
"""
import pytest


@pytest.mark.unit
def test_health_endpoint_returns_200(client):
    """健康端點應回傳 200 狀態碼"""
    response = client.get("/health")
    assert response.status_code == 200


@pytest.mark.unit
def test_health_endpoint_has_required_fields(client):
    """健康端點回應應包含 status、database、cache 欄位"""
    response = client.get("/health")
    data = response.json()
    assert "status" in data
    assert "database" in data
    assert "cache" in data


@pytest.mark.unit
def test_api_health_backward_compat(client):
    """/api/health 向後相容路由應正常回應"""
    response = client.get("/api/health")
    assert response.status_code == 200


@pytest.mark.unit
def test_health_database_connected(client):
    """健康端點的 database 狀態應為 connected（SQLite in-memory 可用）"""
    response = client.get("/health")
    data = response.json()
    assert data["database"] == "connected"
```

#### 4-2：`tests/test_auth.py` — 認證流程

```python
"""
認證相關端點測試
注意：Google Token 驗證需要 mock
"""
import pytest


@pytest.mark.unit
def test_exchange_token_missing_credential(client):
    """未提供 credential 時應回傳 422"""
    response = client.post("/api/auth/exchange-token", json={})
    assert response.status_code == 422


@pytest.mark.unit
def test_token_status_requires_auth(client):
    """未帶 Bearer Token 時，/token-status 應回傳 401 或 403"""
    response = client.get("/api/auth/token-status")
    assert response.status_code in (401, 403)


@pytest.mark.unit
def test_exchange_token_rate_limit_header(client):
    """Token 交換端點應有速率限制（slowapi 注入 headers）"""
    response = client.post(
        "/api/auth/exchange-token",
        json={"credential": "invalid_token"}
    )
    # 無論成功與否，端點應存在（不是 404）
    assert response.status_code != 404
```

#### 4-3：`tests/test_permissions.py` — 權限模組

```python
"""
權限模組基礎測試
"""
import pytest


@pytest.mark.unit
def test_permissions_endpoint_requires_auth(client):
    """/api/permissions 端點應需要認證"""
    response = client.get("/api/permissions")
    assert response.status_code in (401, 403)


@pytest.mark.integration
def test_seed_permissions_creates_modules(db):
    """
    seed_permissions 應正確建立 Module 資料
    """
    from database.models.permission import Module

    # 確認資料表可正常查詢（空的也算通過）
    modules = db.query(Module).all()
    assert isinstance(modules, list)
```

#### 4-4：`tests/test_cache.py` — 快取邏輯

```python
"""
快取模組測試（使用 mock Redis）
"""
import pytest


@pytest.mark.unit
def test_generate_cache_key_consistent(mock_redis):
    """相同參數應生成相同的快取 key"""
    from cache import generate_cache_key

    key1 = generate_cache_key("test", user_id=1, period="30d")
    key2 = generate_cache_key("test", user_id=1, period="30d")
    assert key1 == key2


@pytest.mark.unit
def test_generate_cache_key_different_params(mock_redis):
    """不同參數應生成不同的快取 key"""
    from cache import generate_cache_key

    key1 = generate_cache_key("test", user_id=1, period="30d")
    key2 = generate_cache_key("test", user_id=2, period="30d")
    assert key1 != key2


@pytest.mark.unit
def test_cache_miss_returns_none(mock_redis):
    """快取 miss 時應回傳 None"""
    from cache import get_cached

    result = get_cached("nonexistent_key_xyz")
    assert result is None
```

#### 4-5：`tests/test_integration_service.py` — UserIntegration 服務

```python
"""
integration_service.py 單元測試
測試 CRUD 操作與 Token 加解密
"""
import pytest
from unittest.mock import patch


@pytest.mark.integration
def test_get_user_integration_returns_none_when_not_exists(db, sample_user):
    """查詢不存在的整合應回傳 None"""
    from services.integration_service import get_user_integration

    result = get_user_integration(db, sample_user.id, "facebook")
    assert result is None


@pytest.mark.integration
def test_upsert_and_get_user_integration(db, sample_user):
    """建立整合後應可正確查詢"""
    from services.integration_service import upsert_user_integration, get_user_integration

    # 需要模擬加密環境
    with patch("services.integration_service.encrypt_token", return_value="encrypted_token"):
        upsert_user_integration(
            db,
            user_id=sample_user.id,
            provider="facebook",
            access_token="raw_test_token",
        )

    result = get_user_integration(db, sample_user.id, "facebook")
    assert result is not None
    assert result.provider == "facebook"


@pytest.mark.integration
def test_delete_user_integration(db, sample_user):
    """刪除整合後查詢應回傳 None"""
    from services.integration_service import (
        upsert_user_integration,
        delete_user_integration,
        get_user_integration,
    )

    with patch("services.integration_service.encrypt_token", return_value="encrypted_token"):
        upsert_user_integration(
            db,
            user_id=sample_user.id,
            provider="google",
            access_token="some_token",
        )

    delete_user_integration(db, sample_user.id, "google")
    result = get_user_integration(db, sample_user.id, "google")
    assert result is None
```

---

### Step 5：安裝測試依賴

**檔案**：`backend/requirements-dev.txt`（更新或確認存在以下依賴）

```txt
# 測試框架
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.24.0,<1.0.0
pytest-cov>=6.0.0,<7.0.0
httpx>=0.28.0,<1.0.0          # FastAPI TestClient 的 async 支援

# Mock 工具
pytest-mock>=3.14.0,<4.0.0

# 類型檢查（可選）
mypy>=1.14.0,<2.0.0
```

```powershell
cd backend
pip install -r requirements-dev.txt
```

---

### Step 6：執行測試驗證

```powershell
cd backend

# 執行所有測試
pytest tests/ -v

# 僅執行 unit 測試（不需要 DB）
pytest tests/ -v -m unit

# 執行並輸出覆蓋率報告
pytest tests/ --cov=. --cov-report=term-missing --cov-report=html

# 執行特定測試檔
pytest tests/test_health.py -v
```

**預期輸出**：
```
tests/test_health.py::test_health_endpoint_returns_200 PASSED
tests/test_health.py::test_health_endpoint_has_required_fields PASSED
tests/test_health.py::test_api_health_backward_compat PASSED
tests/test_health.py::test_health_database_connected PASSED
tests/test_auth.py::test_exchange_token_missing_credential PASSED
tests/test_auth.py::test_token_status_requires_auth PASSED
...
```

---

## 測試覆蓋率目標

| 階段 | 目標 | 重點模組 |
|------|------|---------|
| 第一週（本次） | 30% | health、auth、permissions 端點，integration_service |
| 第二週 | 50% | cache 邏輯、teams CRUD、users CRUD |
| 第三週 | 65%+ | Facebook/GSC/GA4（mock httpx 外部呼叫） |

---

## 驗收標準

- [ ] `backend/tests/` 目錄存在
- [ ] `backend/pytest.ini` 存在
- [ ] `pytest tests/ -v` 執行成功（無 ERROR，至少 15 個 PASSED）
- [ ] `pytest tests/ --cov` 覆蓋率達 30% 以上
- [ ] 無測試直接呼叫外部 API（全部 mock）

---

## 注意事項

### SQLite vs PostgreSQL 的測試差異

使用 SQLite in-memory 作為測試 DB 時，需注意以下限制：

```python
# SQLite 不支援某些 PostgreSQL 專有語法，例如：
# - JSONB 欄位（SQLite 會降級為 JSON 字串）
# - gen_random_uuid()
# - partial index （`CREATE INDEX ... WHERE ...`）

# 解決方案：對 PostgreSQL 專有功能使用條件式跳過
@pytest.mark.skipif(
    "sqlite" in str(engine.url),
    reason="PostgreSQL-only feature"
)
def test_jsonb_operations():
    ...
```

### 避免測試污染

```python
# ✅ 正確：每個測試使用 db fixture（自動 rollback）
def test_something(db, sample_user):
    ...

# ❌ 錯誤：直接 import 並使用全域 SessionLocal
def test_something_bad():
    db = SessionLocal()  # 測試資料殘留！
    ...
```
