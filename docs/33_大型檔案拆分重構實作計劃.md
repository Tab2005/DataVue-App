# 33. 大型檔案拆分重構實作計劃

- 建立日期：2026-07-14
- 狀態：第 1-6 波已完成並驗收覆核（2026-07-14）；Phase A-E 全部完成，並於 2026-07-15 完成第二輪覆核（見「十一、驗收標準」與「十三、驗收覆核記錄」之「第二輪覆核記錄」）；第 7 波（P3 選配）未啟動，維持需求驅動
- 依據：docs/31（專案原始碼架構與關聯分析，已依本計劃重新產出，統計流程見「十三」Phase E）
- 性質：純結構重構（behavior-preserving），不改任何業務邏輯、不動資料庫 schema、不改 API 契約

---

## 零、評估方法與結論總覽

拆分優先級不是只看行數，而是「**行數 × 變更熱度 × 內部結構證據**」三者綜合：

- **行數**：docs/31 的統計已過時（例如 `repository.py` 文件記載 2880 行，現況已 3205 行），本文件全部採用 2026-07-14 重新量測的現況數字。
- **變更熱度**：以 `git log --since=2026-05-01` 統計近兩個半月每檔的 commit 次數。大而冷的檔案（如 `GSCStats.jsx` 3717 行但只改過 2 次）拆分收益低、風險高；大而熱的檔案（如 `repository.py` 58 次）每次改動都要在三千行裡找位置、AI 輔助開發時每次都要吃掉大量 context，拆分收益最高。
- **結構證據**：實際檢視每個候選檔案的 class/function 分佈，確認存在自然的拆分邊界（方法群聚），而不是硬拆。

### 候選檔案總表（依優先級排序）

| 優先級 | 檔案 | 現況行數 | 近 2.5 月 commits | 波次 |
|---|---|---:|---:|---|
| P0 | `backend/modules/meta_andromeda/repository.py` | 3,205 | 58 | 第 1 波 |
| P0 | `backend/modules/meta_andromeda/service.py` | 1,887 | 37 | 第 2 波 |
| P0 | `backend/modules/meta_andromeda/runtime.py` | 1,489 | 36 | 第 2 波 |
| P1 | `frontend/src/pages/MetaAndromedaMonitoring.jsx` | 2,379 | 33 | 第 3 波 |
| P1 | `backend/tests/test_meta_andromeda_module.py` | 4,051 | 26 | 第 4 波 |
| P1 | `backend/core/scheduler.py` | 1,181 | 13 | 第 5 波 |
| P2 | `frontend/src/pages/Analytics.jsx` | 2,777 | 16 | 第 6 波 |
| P2 | `frontend/src/pages/ContributionAnalysis.jsx` | 2,305 | 10 | 第 6 波 |
| P2 | `frontend/src/pages/GA4Insights.jsx` | 1,906 | 8 | 第 6 波 |
| P2 | `frontend/src/components/SettingsModal.jsx` | 1,122 | 5 | 第 6 波 |
| P3（選配） | `frontend/src/components/GSCStats.jsx` | 3,717 | 2 | 第 7 波 |
| P3（選配） | `frontend/src/components/GA4Stats.jsx` | 3,184 | 2 | 第 7 波 |
| P3（選配） | `backend/modules/ga4/insights_service.py` | 1,065 | 8 | 第 7 波 |

`meta_andromeda/router.py`（915 行、33 commits）雖然也熱，但 router 天生是「一行端點宣告對應一個 service 呼叫」的薄層，915 行內含大量 docstring 與 response_model 宣告，拆了反而讓端點總覽變難找，**判定不拆**（見「一、明確不拆清單」）。

---

## 一、明確不拆清單（記錄在案，避免之後重複評估）

| 檔案 | 行數 | 不拆理由 |
|---|---:|---|
| `backend/modules/meta_andromeda/router.py` | 915 | 薄層端點宣告，單一檔案就是完整 API 目錄，拆散降低可查性 |
| `backend/modules/meta_andromeda/schemas.py` | 626 | 純 Pydantic 宣告，無邏輯，行數多但認知負擔低 |
| `backend/alembic/versions/*` | — | migration 依定義不可改 |
| `frontend/src/constants/metricsRegistry.js` | 545 | 純資料表 |
| `backend/services/facebook_service.py` | 811 | 0 commits（2026-05 起未動），屬**遺留待淘汰**而非待拆分——fb_ads 模組（`accounts_service`/`analytics_service`/`insights_service`/`trends_service`）就是它的接班架構，剩餘引用應逐步遷移後刪除，另立案處理 |
| `backend/scripts/maintenance/main_legacy.py` | 1,553 | 遺留單體腳本，應歸檔/刪除而非拆分，另立案處理 |
| `backend/modules/contribution/service.py` / `router.py` | 760 / 688 | 尚在健康範圍且該模組已按 engine/grouping/repository/data_source 拆過一輪，暫不動 |

### 順帶發現的重複檔案（建議另立清理案，非本計劃範圍）

盤點過程中發現以下疑似重複/遺留，值得之後開一個小案處理：

1. `backend/exceptions.py` 與 `backend/core/exceptions.py`：兩檔行數（171）與位元組數（5594）完全相同，內容重複，應收斂為 `core/exceptions.py` 單一來源。
2. `backend/modules/ai_hub/`（clients/intent_classifier/service 皆 <50 行）與 `backend/services/ai/`（同名檔案、完整實作）並存，`ai_hub` 看起來是薄轉發層，語意重複。
3. 根目錄 `backend/ga4_service.py`、`backend/gsc_service.py`、`backend/ai_service.py` 與對應 `modules/` 版本並存，屬於模組化改造未收尾的殘留。

---

## 二、共同拆分原則（每一波都適用）

1. **行為保持（behavior-preserving）**：只做「搬移 + re-export」，不改任何函式簽名、不改邏輯、不改例外類型。每一波拆完，既有測試必須以「修改前後失敗集合完全一致」為基準（目前 `test_meta_andromeda_module.py` 有 29 個已知環境性失敗，拆分不得增減）。
2. **Facade 模式保住既有 import 路徑**：後端拆分一律採「單檔 → 同名 package」手法：`repository.py` → `repository/` 目錄 + `__init__.py` re-export 原有公開名稱。所有 `from .repository import MetaAndromedaRepository` 的既有呼叫端**零修改**。
3. **一波一 commit，拆分 commit 不夾帶任何功能修改**：讓 review 時 `git diff --stat` 只看到搬移，方便用 `git log --follow` 追歷史。
4. **先拆熱的，冷的緩拆**：P0 檔案幾乎每個功能案都會碰，先拆立即降低後續所有開發的成本；P3 的 GSCStats/GA4Stats 雖然最大，但近期沒人動它，收益排最後。
5. **前端拆分以「已存在的目錄慣例」為家**：`components/Analytics/`、`components/Reports/`、`components/Landing/` 已是既有慣例，新拆出的元件比照建立 `components/MetaAndromeda/`、`components/GA4Insights/`、`components/Contribution/` 等子目錄。
6. **每波拆完跑對應驗證**：後端跑 `pytest` 對應測試檔 + `python -c "import ..."` 冒煙；前端跑 `npm run build` + 既有 vitest 測試（`ContributionAnalysis.test.jsx` 等 5 個頁面測試是現成的回歸保護網）。

---

## 三、第 1 波（P0）：`meta_andromeda/repository.py`（3,205 行 → 8 個檔案）

全專案最大、最熱的業務檔案。內部結構證據明確——模組層函式與 `MetaAndromedaRepository` 類別的方法已自然群聚成八個領域，拆分邊界幾乎不需要發明：

### 目標結構

```
backend/modules/meta_andromeda/repository/
├── __init__.py          # re-export：MetaAndromedaRepository、compute_release_metrics、
│                        #   ReleaseGateError、PromotionGateError、ReleaseCandidateExistsError 等
│                        #   全部原有公開名稱，既有 import 零修改
├── _stats.py            # 純統計函式（無 DB 依賴）
├── release_metrics.py   # release/backtest 指標計算（模組層函式）
├── review_queue.py      # ReviewQueueMixin
├── monitoring.py        # MonitoringMixin
├── drift.py             # DriftMixin
├── score_events.py      # ScoreEventMixin
├── observations.py      # ObservationMixin
├── release.py           # ReleaseMixin
├── feedback_calibration.py  # FeedbackCalibrationMixin
└── profiles_registry.py     # ProfileRegistryMixin
```

`MetaAndromedaRepository` 在 `__init__.py` 以 mixin 組裝：

```python
class MetaAndromedaRepository(
    ReviewQueueMixin, MonitoringMixin, DriftMixin, ScoreEventMixin,
    ObservationMixin, ReleaseMixin, FeedbackCalibrationMixin, ProfileRegistryMixin,
):
    pass
```

### 內容分配（依現況行號）

| 新檔案 | 搬入內容（現況行號） |
|---|---|
| `_stats.py` | `_compute_pairwise_ranking_accuracy`(229)、`_average_rank`(423)、`_spearman_r`(440)、`_spearman_r_weighted`(464)、`_classify_period_state`(513)、`_window_days`(111)、`_dedupe_observed_by_ad_id`(120) |
| `release_metrics.py` | `ReleaseGateError`(49)、`_release_min_pairwise_accuracy`(58)、`_assert_release_gate`(73)、`_collect_release_metric_pairs`(246)、`compute_release_metrics`(306)、`list_release_metric_pairs`(328)、`_collect_backtest_metric_pairs`(360)、`compute_backtest_run_metrics`(409) |
| `review_queue.py` | `list_review_queue`(1026)、`get_review_queue_detail`(1171)、`delete_score_event`(995)、`batch_delete_score_events`(1007)、序列化 helpers `_score_to_list_item`(860)、`_score_to_detail`(912)、`_safe_json_dict`(897) |
| `monitoring.py` | `get_monitoring_summary`(1265)、`get_score_event_timeline`(1433)、`list_observed_accounts`(2892)、`get_drift_trend`(2919)、`_worker_event_to_dict`(929)、`_dead_letter_to_dict`(944) |
| `drift.py` | `create_drift_report`(1474，本體 365 行)、`_drift_report_to_dict`(958)、`_check_promoted_profile_degradation`(144) |
| `score_events.py` | `create_score_event`(2049)、`assign_runtime_job`(2092)、`log_worker_event`(2102)、`find_worker_event`(2129)、`create_dead_letter`(2151)、`mark_score_processing/completed/failed`(2176-2246)、`requeue_score_event`(2247) |
| `observations.py` | `ensure_seed_data`(819)、`get_asset_by_uri`(1943)、`create_uploaded_asset`(1947)、`create_observed_creative`(1966)、`get_observed_creative`(2010)、`get_latest_score_event_for_observation`(2026) |
| `release.py` | `get_release_overview`(1839)、`_release_record_to_dict`(1911)、`_release_event_to_dict`(1929)、`create_release_candidate`(2416)、`perform_release_action`(2527)、backtest CRUD(2630-2706)、`refresh_release_metrics`(2707)、`_switch_model_registry_production`(197)、`_backtest_run_to_dict`(972)、`PromotionGateError`(3191)、`ReleaseCandidateExistsError`(3200) |
| `feedback_calibration.py` | `list_feedback`(2259)、`submit_feedback`(2285)、`list_feedback_calibration_candidates`(2341)、`analyze_feedback_reason_codes`(2369)、`sync_calibration_dataset`(2731) |
| `profiles_registry.py` | `get_active_base_profile_name`(2967)、`list_scoring_profiles`(2987)、`promote_scoring_profile`(3011)、`list_model_registry_entries`(3080)、`get_effective_scoring_status`(3100)、`set_backtest_reference_model`(3145) |

### 執行步驟

1. 建立 `repository/` 目錄與空的 mixin 骨架，先確認 `from .repository import MetaAndromedaRepository` 在 package 形式下可運作（`repository.py` 改名為 `repository/__init__.py` 這一步 git 要用 `git mv` 保 blame）。
2. 按上表逐檔搬移，共用的模組層 import 各自帶走，`__init__.py` 統一 re-export。
3. 特別注意三處跨群聚呼叫：`get_review_queue_detail` 用到 `labeling.compute_label_thresholds`（2026-07-14 修的路徑）；`create_drift_report` 呼叫 `_check_promoted_profile_degradation` 與 `persist_label_policy`；`perform_release_action` 呼叫 `_assert_release_gate` 與 `_switch_model_registry_production`。這些維持函式層 import，不做額外抽象。
4. 驗證：`pytest tests/test_meta_andromeda_module.py`（失敗集合須與拆前完全一致）+ `python -c "from modules.meta_andromeda.repository import MetaAndromedaRepository, compute_release_metrics"`。
5. 檢查 worker 路徑：`worker_main.py` → `internal_router` 不直接 import repository，但 `queue_host.py`/`scheduler.py` 有間接依賴，部署後需確認 backend 與 meta-andromeda-worker 兩個服務都正常啟動。

實作狀態：已完成（commit a1acaf8）。實際落地為 `_shared.py`(共用 import/常數) + `_stats.py` + 8 個 mixin 檔，比計劃多一個 `_shared.py`，其餘檔案切分與計劃表一致。`__init__.py` re-export 全部原有公開名稱，既有 import 零修改（已逐一 grep 驗證呼叫端）。

---

## 四、第 2 波（P0）：`service.py`（1,887 行）與 `runtime.py`（1,489 行）

### 4.1 `service.py` → `service/` package（6 個檔案）

`MetaAndromedaService` 是單一巨型 static-method 類別，方法已群聚成五個領域。同樣採 package + facade：`__init__.py` 保留 `MetaAndromedaService` 與 `MetaAndromedaValidationError`，方法本體搬到子模組後由類別委派（或 mixin 組裝，與第 1 波同手法）。

| 新檔案 | 搬入內容（現況行號） |
|---|---|
| `assets.py` | `upload_asset`(338)、`_compress_image`(366)、`_validate_uploaded_asset`(441)、`_is_allowed_media_host`(475)、`get_asset_by_uri`(334) |
| `observation_import.py` | `build_observed_creative_id`(508) 至 `get_observed_facebook_ad_import_status`(972) 的完整匯入管線（queue/dispatch/status/sync helpers），約 500 行，是最大的一塊 |
| `worker_callbacks.py` | `_build_external_worker_signature`(1050) 至 `_complete_score_event_sync`(1428)：簽章、驗證、外部 worker callback 處理、score event 狀態機 |
| `scoring.py` | `create_score_event`(742)、`enqueue_score_event`(1025)、`assign_score_runtime_job`(1021)、`get_score_detail`(1605) |
| `admin_ops.py` | `cleanup_stale_score_events`(1499)、feedback 三連(1609-1636)、release/backtest 委派(1640-1848)、`list_scoring_profiles`(1849)、監控/總覽委派(56-330) |

注意：`_build_backtest_score_payload`(1719) 與 backtest 委派放一起進 `admin_ops.py`；`_clear_observation_import_status_entries`(1495) 跟著 `cleanup_stale_score_events` 走。

### 4.2 `runtime.py` → 抽出四個支援模組（runtime.py 本體保留 adapter 與 provider）

`runtime.py` 被 backend 與 worker 兩個服務共用，拆分時**不改變 `runtime.py` 這個模組路徑本身**，只把支援函式抽出去、原檔 re-export：

| 新檔案 | 搬入內容（現況行號） |
|---|---|
| `prompt_profiles.py` | `_DEFAULT_OBJECTIVE_PROFILES`(151，160 行的大常數)、`_load_scoring_profile`(310)、`_resolve_active_profile`(387)、prompt cache 失效函式(440-460) |
| `result_parsing.py` | `_strip_reasoning_blocks`(501)、`_extract_json_payload`(508)、`_normalize_string_list`(567)、`_normalize_diagnostic_breakdown`(579)、`_validate_provider_result`(703) |
| `confidence.py` | `_compute_signal_completeness`(636)、`_compute_confidence`(651)、self-consistency 四函式(809-877) |
| `heuristic.py` | `build_heuristic_score_result`(1092，177 行) |

`runtime.py` 拆後剩下約 700 行：providers（`BaseScoringProvider`/`HeuristicScoringProvider`/`OpenRouterScoringProvider`）+ `MetaAndromedaRuntimeAdapter` + 多模態組裝，這是合理的單一職責範圍。

驗證：除 pytest 外，加跑 `backend/scripts/meta_andromeda_shared_runtime_smoke.py`（現成的 runtime 冒煙腳本）。

實作狀態：已完成（commit 5247d77，與 4.1 同一個 commit 一起送出）。`service/` package 落地為 `_shared.py` + `assets.py`/`observation_import.py`/`worker_callbacks.py`/`scoring.py`/`admin_ops.py` 5 檔，與計劃表一致；`runtime.py` 支援模組（`prompt_profiles.py`/`result_parsing.py`/`confidence.py`/`heuristic.py`）已抽出至 `modules/meta_andromeda/` 頂層（而非 `runtime/` 子目錄），`runtime.py` 本體降至 572 行。**已知落差**：`service/_shared.py` 的 `SessionLocal` 是各子模組各自 `from ._shared import *` 取得的獨立綁定，測試若以 `monkeypatch.setattr(service_module, "SessionLocal", ...)` 只打在 package 層級，不會影響 `worker_callbacks.py` 等子模組內已綁定的名稱——這造成 `test_meta_andromeda_score_retries_then_completes` 迴歸（詳見十一之 2、十三）。

---

## 五、第 3 波（P1）：`MetaAndromedaMonitoring.jsx`（2,379 行，前端最熱）

單一元件 2,224 行（22-2245）+ 一個 12 行的 `Metric` 小元件。頁面實際是多個獨立監控面板的堆疊，拆分邊界照面板走：

### 目標結構

```
frontend/src/components/MetaAndromeda/
├── MonitoringSummaryCards.jsx      # 摘要統計卡列
├── DriftTrendPanel.jsx             # 漂移趨勢圖 + drift 觸發
├── ScoringProfilesPanel.jsx        # 評分設定檔管理
├── ModelRegistryPanel.jsx          # 模型註冊表 + 候選驗證 + backtest 模型設定
├── WorkerEventsPanel.jsx           # worker 事件 / 死信列表
├── FeedbackAnalysisPanel.jsx       # 回饋校準候選 / reason code 分析
└── shared.jsx                      # Metric 等共用小元件
frontend/src/hooks/
└── useMetaAndromedaMonitoring.js   # 資料抓取邏輯（目前散在元件內的 fetch + state）
```

頁面本體 `MetaAndromedaMonitoring.jsx` 降為 <300 行的組合層。

### 執行注意

1. 既有測試 `__tests__/MetaAndromedaMonitoring.test.jsx`（203 行）是回歸保護網，拆分後必須全綠且不修改斷言。
2. 此頁狀態耦合度高（多個面板共用 account 選擇與刷新 timer），先抽 hook、再抽面板，兩步分開 commit。
3. 拆分時同步確認 `usePermission` 的守門邏輯在每個面板內維持原樣（參照 memory：前端 usePermission 需比照後端複合守門，不能拆丟）。

實作狀態：已完成但**未依計劃拆到面板粒度**（commit 996f542）。實際只拆出 `MonitoringDashboard.jsx`（1,804 行，單一大檔，未依計劃再拆成 6 個 `*Panel.jsx`）+ `shared.jsx`（134 行）+ `useMetaAndromedaMonitoring.js`（621 行）。`MetaAndromedaMonitoring.jsx` 頁面本體降至 20 行組合層，達成目標；但 `MonitoringDashboard.jsx` 遠超「前端面板元件 <600 行」的驗收標準（見十一之 5）。既有測試 `MetaAndromedaMonitoring.test.jsx` 驗證：現況通過（比拆分前的 baseline 還多修好一個原本失敗的案例，見十三）。

---

## 六、第 4 波（P1）：`test_meta_andromeda_module.py`（4,051 行 → 6 個測試檔）

單一測試檔 4 千行，pytest 收集慢、定位難、且 29 個已知環境性失敗混在其中難以分類管理。按被測領域拆：

```
backend/tests/meta_andromeda/
├── conftest.py                 # 從現有檔案抽出共用 fixtures/mock scorer/DB 建構
├── test_scoring_pipeline.py    # 評分事件生命週期、provider 驗證、heuristic fallback
├── test_review_queue.py        # 列表/明細/刪除/篩選
├── test_observation_import.py  # 成效分析匯入、facebook importer、視窗契約
├── test_drift_calibration.py   # drift report、labeling、校準集同步、label policy
├── test_release_backtest.py    # release 閘門、backtest runs、指標計算
└── test_assets_misc.py         # 上傳/壓縮/preview/權限、其餘雜項
```

拆完順手把 29 個已知環境性失敗標上 `@pytest.mark.skipif`（缺 PIL、缺 storage migration 等具體條件），讓「預期失敗」不再污染每次跑測試的訊號——這是本計劃唯一允許的非純搬移修改，單獨一個 commit。

實作狀態：目錄拆分已完成（commit 092c0ba），結構與計劃一致（`conftest.py` + 6 個 `test_*.py`）。**skipif 標記尚未執行**——現況跑下來仍是 21 個 `FAILED`（非 `SKIPPED`），這件「順手」的收尾工作還沒做（見十一之 2）。

---

## 七、第 5 波（P1）：`core/scheduler.py`（1,181 行）

排程核心 + 四個領域的 job 全部擠在一檔。拆為：

```
backend/core/scheduler/
├── __init__.py        # re-export 全部既有公開名稱（scheduler 實例、is_scheduler_enabled、
│                      #   get_job_id、各 add_*_job——呼叫端遍布 main.py/worker_main.py/多個 service）
├── engine.py          # scheduler 實例、trigger 建構(69-164)、事件監聽(168-215)、狀態查詢
├── report_jobs.py     # process_scheduled_report(240)、add_report_job(532)、resolve_schedule_user(216)
├── meta_andromeda_jobs.py  # score/observation/backtest/calibration/queue sweeper/redis stream 全部 MA jobs(364-418, 461-531, 578-648, 765-841)
├── contribution_jobs.py    # process_contribution_analysis(419)、stale sweep(430)、add jobs(649-701)
└── ga4_jobs.py             # intraday/daily insights jobs(702-764)
```

**風險註記**：全域 `scheduler` 單例與 job 註冊順序是這檔的隱形契約，`engine.py` 必須是唯一持有實例的地方，各 `*_jobs.py` 從 engine import。`worker_main.py` 的 SERVICE_ROLE 分支邏輯（哪些 job 只在 worker 跑）拆分時逐一核對，這是 docs/24/25 處理過的雷區。

實作狀態：已完成（commit b358c30），結構與計劃一致（`_shared.py` + `engine.py`/`report_jobs.py`/`meta_andromeda_jobs.py`/`contribution_jobs.py`/`ga4_jobs.py`）。既有 `from core.scheduler import ...` 呼叫端零修改（已 grep 驗證）。`tests/test_scheduler.py` 有 1 個既存失敗（`test_get_next_run_time_uses_weekly_schedule`，day_of_week 計算與 APScheduler 版本行為有關），經核對在拆分前的 baseline commit 上同樣失敗，**與本次拆分無關**。

---

## 八、第 6 波（P2）：其餘前端大頁

### 8.1 `ContributionAnalysis.jsx`（2,305 行）——最容易，先做暖身

檔內**已經**定義好 15 個子元件（`SelfDoubtfulBadge`/`Section`/`ContributionChart`/`ContributionTable`/`MarginalChart`/`DiagnosticsPanel`/`AiInsightsCard`/`GroupEditor`/`HistoryList`/`AnalysisView` 等，行號 221-1714），只是全部塞在同一檔。搬到 `components/Contribution/` 一檔一元件即可，頁面本體(1715-2305)不動。既有 734 行測試檔為保護網。

實作狀態（第 6 波 8.1）：已先以低風險方式把頁面內 UI 子元件與 UI 專用工具集中搬到 `frontend/src/components/Contribution/ContributionAnalysisComponents.jsx`，`ContributionAnalysis.jsx` 保留資料載入、輪詢、狀態管理與頁面組裝。頁面檔由 2,305 行降至約 674 行。驗證：`npm test -- ContributionAnalysis.test.jsx`、`npm run build` 通過（2026-07-14 複核：build 仍通過）。**已知落差**：`ContributionAnalysisComponents.jsx` 現況為 1,646 行的單一集中檔（未依原計劃拆成 15 個獨立元件檔），超出「前端面板元件 <600 行」驗收標準；`ContributionAnalysis.jsx` 674 行也超出「頁面組合層 <400 行」標準。

### 8.2 `Analytics.jsx`（2,777 行）

單一元件。`components/Analytics/` 目錄已存在（KPICard/TableRow/MetricSelector/ReportModal），把頁面內的表格區、篩選列、匯入評分對話框、趨勢區依現有慣例繼續抽進去；資料層抽 `hooks/useAnalyticsData.js`。注意此頁含「匯入 Meta Andromeda 評分」流程（`metaAndromedaWorkflowService`），拆分時保持該流程完整在一個元件內。

實作狀態（第 6 波 8.2）：已抽出 `frontend/src/hooks/useAnalyticsData.js`（saved views、analytics data fetch、比較區間資料）、`frontend/src/components/Analytics/analyticsMetrics.js`（metric groups/registry merge）、`AnalyticsDataTable.jsx`（資料表與 hover preview）與 `MetaAndromedaImportActions.jsx`（Meta Andromeda 批次匯入操作列）。`Analytics.jsx` 由 2,777 行降至約 1,740 行（後續 commit ca7f032 微調邏輯，現況 1,848 行）。驗證：`npm run build` 通過（2026-07-14 複核）；目前未找到既有 `Analytics` vitest 測試檔。**已知落差**：頁面組合層現況 1,848 行，遠超「前端頁面組合層 <400 行」驗收標準，是六個第 6 波頁面中離目標最遠的一個。

### 8.3 `GA4Insights.jsx`（1,906 行）

已有 3 個檔內子元件（`AIInsightNote`/`IntradayMetricCard`/`KpiPacingCard`，266-465），主體約 1,440 行是多個 insight 區塊（即時、當日進度、通路、到達頁、商品、KPI）。建 `components/GA4Insights/` 按區塊拆。

實作狀態（第 6 波 8.3）：已建立 `frontend/src/components/GA4Insights/`，抽出 `GA4InsightsShared.jsx`（共享樣式、格式化、AI 解讀卡、Intraday/KPI 卡）以及 `OverviewTab.jsx`、`ChannelsTab.jsx`、`LandingPagesTab.jsx`、`ItemsTab.jsx`、`KpiTab.jsx`、`AlertsTab.jsx`。`GA4Insights.jsx` 由 1,906 行降至約 757 行，保留資料載入、狀態與分頁組裝。驗證：`npm run build` 通過（2026-07-14 複核）；目前未找到既有 `GA4Insights` vitest 測試檔。各拆出面板檔均 <500 行，達成面板 <600 行標準；頁面組合層 757 行則超出 <400 行標準。

### 8.4 `SettingsModal.jsx`（1,122 行）

按設定分頁拆（整合連結、API 金鑰、LINE 綁定已有 `Settings/LineBindingCard.jsx` 先例），比照建 `Settings/` 子元件。

實作狀態（第 6 波 8.4）：已在 `frontend/src/components/Settings/` 抽出 `ActiveAiProviderSelector.jsx`（AI 模組切換列）、`SettingsTabs.jsx`（設定分頁列）、`FacebookSettingsTab.jsx`（Facebook Ads token/API 表單）、`ZeaburAiSettingsTab.jsx`（Zeabur AI Hub 設定）與 `OpenRouterSettingsTab.jsx`（OpenRouter API key/模型設定），並沿用既有 `LineBindingCard.jsx`。`SettingsModal.jsx` 由 1,122 行降至約 579 行，保留 modal 容器、資料載入與儲存流程；同時修正 Zeabur provider 初始化時切到不存在 `zeabur` 分頁的問題，改為既有 `ai` 分頁。驗證：`npm run build` 通過（2026-07-14 複核）；目前未找到既有 `SettingsModal`/`Settings` vitest 測試檔。各拆出面板檔均 <260 行，達成面板 <600 行標準；頁面組合層 579 行則超出 <400 行標準。

---

## 九、第 7 波（P3，選配）：大而冷的檔案

- `GSCStats.jsx`（3,717 行）與 `GA4Stats.jsx`（3,184 行）：全專案最大的兩個檔案，但近 2.5 月各只有 2 個 commits。兩者結構相同——頂部 `TABS` 常數 + 單一巨型元件，拆法同第 3 波（按 tab 拆面板 + 抽資料 hook）。**建議等到下次需要實質修改這兩頁時再順勢拆**，避免為不動的程式碼承擔回歸風險。
- `modules/ga4/insights_service.py`（1,065 行）：`GA4InsightsService` 方法已群聚（rules CRUD / dashboard 快照 / channels-landing-items 分析 / KPI targets），若 GA4 洞察功能持續迭代（docs/22 還有後續），屆時按此邊界拆為 package。

---

## 十、建議執行順序與相依

```
第 1 波 repository.py     ← 立即收益最大，先做
  └→ 第 2 波 service.py + runtime.py   （依賴第 1 波的 import 路徑穩定）
第 3 波 MetaAndromedaMonitoring.jsx    （與後端波次無相依，可並行）
第 4 波 測試檔拆分                      （建議在 1、2 波之後，拆測試時順便按新模組邊界分組）
第 5 波 scheduler.py                   （獨立，可隨時插入，但建議避開有排程功能開發的期間）
第 6 波 其餘前端頁                      （8.1 ContributionAnalysis 最簡單，可提前當暖身）
第 7 波 選配                            （需求驅動，不主動排程）
```

每一波之間部署一次驗證，不累積多波一起上。

## 十一、驗收標準

（2026-07-14 針對第 1-6 波逐項複核，詳細過程見「十三、驗收覆核記錄」）

1. [x] 每波拆分 commit 為純搬移（`git diff` 無邏輯變更），且 `git mv` 保留 blame 連續性
   驗證：`git show --stat` 逐一檢查 8 個拆分 commit，均為「刪原檔 + 新增子檔案」的乾淨搬移，未夾帶無關邏輯改動。
2. [ ] 後端：`pytest` 失敗集合與拆分前完全一致；backend 與 meta-andromeda-worker 兩服務部署後正常啟動
   **未完全達成**：以 git worktree 重建拆分前基準（commit 332b7f7）比對 `tests/meta_andromeda/`（現況 105 案，21 failed / 84 passed）against `tests/test_meta_andromeda_module.py`（基準 105 案，24 failed / 81 passed）：
   - 4 案由「失敗→通過」（bonus 修復，非本計劃預期範圍，需另外確認是否為刻意修復）
   - 1 案由「通過→失敗」（**真迴歸**）：`test_meta_andromeda_score_retries_then_completes`。根因：測試以 `monkeypatch.setattr(meta_andromeda_service_module, "SessionLocal", ...)` 打在 `service/__init__.py` 這個 package 層級，但拆分後 `service/worker_callbacks.py` 是透過 `from ._shared import *` 各自綁定一份 `SessionLocal`，package 層的 monkeypatch 打不到子模組內已綁定的名稱，導致背景重試路徑改用真實 `SessionLocal()`，打到本機未跑過 migration 的 `facebook_dashboard.db`（缺 `started_at` 等欄位）而炸掉。**這是本次拆分暴露的真實 bug，尚未修復。**
   - `tests/test_scheduler.py::test_get_next_run_time_uses_weekly_schedule` 在基準與現況皆失敗（day_of_week 計算與 APScheduler 版本行為有關），與本次拆分無關，不計入迴歸。
   - `python -c "from ... import ..."` 冒煙、import 路徑檢查全數通過（見第 3 項）。
   - 尚未實際部署 backend / meta-andromeda-worker 兩服務驗證啟動狀態（僅本機驗證）。
3. [x] 既有 import 路徑零破壞：`from modules.meta_andromeda.repository import ...`、`from core.scheduler import ...`、`from modules.meta_andromeda.service import ...` 等呼叫端不需修改
   驗證：grep 全 backend 呼叫端（router/worker_main/queue_host/scheduler jobs 等 30+ 個檔案）與 `python -c` 冒煙 import 全數成功。
4. [ ] 前端：`npm run build` 通過；既有 5 個頁面測試（Contribution/Monitoring/Release/ReviewQueue/ScoreLab）全綠且未修改斷言
   **部分達成**：`npm run build` 通過。vitest 現況 3 failed / 29 passed（2 個檔案有失敗），優於拆分前基準的 4 failed / 28 passed（3 個檔案有失敗）——Monitoring 的既存失敗案例拆分後意外修好、無新增失敗。但 ReviewQueue「submits reviewer feedback with parsed reason codes」與 ScoreLab 兩案在基準與現況皆失敗，屬**既存缺陷**（非本計劃引入），尚未達到「全綠」。
5. [ ] 拆分後單檔行數：後端目標 <800 行、前端頁面組合層 <400 行、前端面板元件 <600 行
   後端：本計劃已拆分目標達成，所有拆分後檔案 <710 行（最大為 `service/admin_ops.py` 704 行）；docs/31 另列現況中仍超過 800 行的剩餘候選（例如 `ga4/insights_service.py`、`meta_andromeda/router.py`、`facebook_service.py`）。
   前端頁面組合層：`MetaAndromedaMonitoring.jsx` 已達標；`Analytics.jsx`、`ContributionAnalysis.jsx`、`GA4Insights.jsx` 依 Phase D 決策列為「已接受/觀察中的組合層例外」，不是本輪阻塞項。
   前端面板元件：Phase D 補強後 `ContributionAnalysisComponents.jsx` 降至 13 行 re-export，`MonitoringDashboard.jsx` 降至 69 行組合層，新增 Monitoring panel/drawer 全數 <600 行；docs/31 另列既有大型元件候選（例如 `GSCStats.jsx`、`GA4Stats.jsx`、`ReportConfig.jsx`）。
6. [x] docs/31 於全部波次完成後重新產出
   **已完成（2026-07-15 初次產出；2026-07-15 第二輪覆核補上可重跑腳本並修正整體統計口徑）**：docs/31 已以 `scripts/validation/source_stats.ps1` 重新產出，納入第 1-6 波與 Phase D 補強後的現況，並標記接受例外與待拆候選。詳見「十三」Phase E。

## 十二、風險與緩解

| 風險 | 緩解 |
|---|---|
| repository/service 拆分期間有功能案並行，產生大範圍衝突 | 每波控制在 1-2 天內完成合併；波次啟動前確認無進行中的 meta_andromeda 功能分支 |
| runtime.py 被 worker 共用，拆錯 import 造成 worker 靜默降級到 heuristic | 拆後必跑 `meta_andromeda_shared_runtime_smoke.py`；部署後檢查 worker 日誌無 import error，並抽查一筆評分確認 `scoring_mode=ai` |
| scheduler 拆分破壞 job 註冊順序或 SERVICE_ROLE 分工 | engine 單例唯一化；對照 docs/24/25 的 worker 架構筆記逐 job 核對 |
| 前端拆分遺失 usePermission 守門 | 每個拆出的面板保留原有權限判斷；對照 memory 中 require_permission 的複合守門原則 |
| mixin 組裝讓方法定義位置變難找 | `__init__.py` 中為每個 mixin 加一行註解標明領域；IDE 的 go-to-definition 不受影響 |
| 子模組各自 `from ._shared import *` 綁定共用名稱（如 `SessionLocal`），導致外部對 package 層 `monkeypatch`/`setattr` 打不到子模組內的實際綁定 | 已在本次驗收中發現一起實例（見十一之 2）；日後拆分含有「背景工作/延遲執行且需要測試 monkeypatch 攔截」的函式時，優先改用 `import _shared` 後以 `_shared.NAME` 存取（保留單一綁定來源），或整理測試改成 patch 子模組本身 |

---

## 十三、驗收覆核記錄（2026-07-14）

本節記錄第 1-6 波完成後、依「十一、驗收標準」逐項複核的方法與結果，供之後追蹤未竟事項。

### 覆核方法

- 以 `git worktree` 在 `332b7f7`（本計劃文件剛建立、尚未開始任何拆分的 commit）重建拆分前基準，複製 `.env` 後分別跑後端 `pytest`、前端 `vitest`，與現況（HEAD）逐案比對失敗集合，而非只看失敗總數。
- 後端 import 路徑以 `Grep` 掃全 backend 呼叫端 + `python -c "from ... import ..."` 冒煙驗證。
- 前端以 `npm run build` + `npx vitest run` 驗證，並比對 baseline 的個別失敗案例名稱。
- 驗完後 `git worktree remove --force` 清乾淨，未對主目錄留下殘留狀態。

### 主要發現

1. **1 個真迴歸**：`test_meta_andromeda_score_retries_then_completes`（詳見十一之 2 的根因分析）。屬於拆分過程中暴露的 `monkeypatch` 目標失效問題，建議另立小案修復（例如把 `service/_shared.py` 的 `SessionLocal` 改成透過模組屬性存取，或請測試改 patch `worker_callbacks.SessionLocal`）。
2. **5 個 bonus 修復**（4 後端 + 1 前端），拆分過程中意外修好，未在任何 commit message 中特別說明原因，建議之後有空時確認是哪個拆分動作的副作用（懷疑與 wave 1 提到的「`labeling.compute_label_thresholds` 路徑修正」及 conftest 重整有關），避免誤以為是巧合而漏記。
3. **2 組前端檔案未達面板 <600 行標準**：`MonitoringDashboard.jsx`（1,804 行）、`ContributionAnalysisComponents.jsx`（1,646 行）。這兩波拆分選擇了「集中搬移到 1 個大檔」而非計劃原訂的「一檔一元件」，達成了降低頁面本體行數的短期目標，但未達成可讀性/認知負擔的根本目的，建議列為後續二次拆分的候選。
4. **前端頁面組合層全數超過 <400 行目標**（除 Monitoring 外），顯示這個驗收指標對於「保留完整資料載入/狀態管理邏輯在同一檔案」的前端頁面來說可能訂得過嚴，需要與使用者確認是否調整標準，或是否要繼續往下拆資料層 hook。
5. **docs/31 尚未重新產出**，是本次唯一「完全沒有動工」的驗收項目。

### 覆核後續實作方案

此方案直接沿用本節「覆核方法」：每一項修復都先建立或引用拆分前基準，再以現況同一組指令驗證，避免把既存失敗誤判為本次修復成果，也避免只用 build 通過掩蓋 runtime no-undef 類問題。

#### Phase A：修復唯一真迴歸（P0）

**狀態**：已完成（2026-07-14）。`worker_callbacks.py` 已改用 `_shared.SessionLocal()` 作為背景 callback 的單一 DB session 來源；相關測試改 patch `meta_andromeda_service_shared_module.SessionLocal`。同次補上 `/runtime-health` 舊路徑相容 alias，並讓 score runtime timeout 走既有 retry/failure/dead-letter 流程而非直接 raise。驗證：`test_meta_andromeda_score_retries_then_completes` 通過；`test_scoring_pipeline.py` + `test_meta_andromeda_concurrency.py` 共 31 passed；`MetaAndromedaService` import 冒煙通過。

**目標**：讓 `test_meta_andromeda_score_retries_then_completes` 回到拆分前基準狀態，且不改變 service package 對外 import 介面。

**實作任務**：
1. 在 `backend/modules/meta_andromeda/service/worker_callbacks.py` 與相關子模組中，把會被測試或背景工作替換的共用依賴（優先 `SessionLocal`）從 `from ._shared import *` 的本地綁定，改為 `from . import _shared` 後以 `_shared.SessionLocal` 存取；若只影響單一測試，也可先改測試 patch 目標為實際使用子模組，但長期建議收斂到單一依賴來源。
2. 保留 `modules.meta_andromeda.service` package 層原有 re-export 行為，避免 router、worker、scheduler 呼叫端需要改 import。
3. 補一個針對背景 callback dependency patch 的最小迴歸測試，或至少把既有 failing test 作為 guard。

**驗收指令**：
- `pytest backend/tests/meta_andromeda/test_scoring_pipeline.py::test_meta_andromeda_score_retries_then_completes`
- `pytest backend/tests/meta_andromeda/test_scoring_pipeline.py backend/tests/test_meta_andromeda_concurrency.py`
- `python -c "from modules.meta_andromeda.service import MetaAndromedaService"`

**完成標準**：該測試通過；後端失敗集合不比基準新增；service package import 冒煙通過。

#### Phase B：建立覆核自動化腳本（P1）

**狀態**：已完成（2026-07-14）。新增 `scripts/validation/refactor_acceptance.ps1`，支援 `-BaselineSha`、`-CurrentRef`、`-Frontend`、`-Backend`、`-Cleanup` 與 `-SkipExecution`；腳本會建立 baseline/current 臨時 worktree、複製必要 env 檔、執行 pytest/vitest/build、解析 failed tests，輸出 baseline/current failures、new regressions、fixed existing failures 與 not-run reason。驗證：PowerShell syntax parse 通過；`-Backend -SkipExecution -Cleanup` smoke 通過，且 `git worktree list` 確認未留下臨時 worktree。

**目標**：把本次手動覆核方法固化，之後每一波拆分都能重跑同一套 baseline/current 比對。

**實作任務**：
1. 新增 `scripts/validation/refactor_acceptance.ps1`（或同目錄 Python 腳本），參數包含 `baseline_sha`、`current_ref`、`frontend`、`backend`、`cleanup`。
2. 腳本使用臨時 `git worktree` checkout baseline，複製必要 `.env` 或提示缺失，分別執行後端 pytest、前端 vitest/build，輸出 JUnit 或純文字摘要。
3. 摘要格式固定為：baseline failed tests、current failed tests、new regressions、fixed existing failures、not-run reason。
4. 把 sandbox 內 `npm run build` 可能因 Vite/esbuild 讀取上層目錄失敗的情況記入腳本說明：本機/CI 應在非 sandbox shell 或 CI runner 執行。

**驗收指令**：
- `powershell -ExecutionPolicy Bypass -File scripts/validation/refactor_acceptance.ps1 -BaselineSha 332b7f7 -CurrentRef HEAD -Frontend -Backend`

**完成標準**：可重現第十三節的「1 真迴歸、bonus 修復、既存前端失敗」分類；worktree 清理後 `git status --short` 不出現腳本產物。

#### Phase C：前端 runtime no-undef 防線（P1）

**狀態**：已完成（2026-07-14）。新增 `frontend/src/pages/__tests__/Analytics.test.jsx`，以 mocked outlet context、權限 hooks、analytics data hook、Meta Andromeda workflow service 與重型子元件完成 `/analytics` account-level render smoke test；若 `importObservationRow`、`observationWindowKind` 這類 render-time 變數漏接，測試會在 render 階段失敗。`scripts/validation/refactor_acceptance.ps1` 的 frontend 流程已新增單檔 ESLint 門檻，覆蓋 `Analytics.jsx`、`ContributionAnalysis.jsx`、`GA4Insights.jsx`、`SettingsModal.jsx`、`MetaAndromedaMonitoring.jsx`，並將 baseline/current lint exit code 納入摘要與 regression gate。

**目標**：避免第 6 波 8.2 這類 `importObservationRow`、`observationWindowKind` 漏接變數只在瀏覽器 runtime 才爆出。

**實作任務**：
1. 對已拆分的大型前端頁逐頁跑 ESLint 單檔檢查，至少覆蓋 `Analytics.jsx`、`ContributionAnalysis.jsx`、`GA4Insights.jsx`、`SettingsModal.jsx`、`MetaAndromedaMonitoring.jsx`。
2. 在覆核腳本中加入 `npx eslint src/pages/Analytics.jsx ...` 或更廣的 `npm run lint`；若全專案 lint 目前有既存 warning/error，先以「本次觸及檔案 lint 無 error」作為過渡門檻。
3. 為 Analytics 補最小 render smoke test：mock `useOutletContext`、權限 hooks 與 service，確認 `/analytics` 頁在 `level='account'` 初始狀態不進 ErrorBoundary。

**驗收指令**：
- `npx eslint src/pages/Analytics.jsx src/pages/ContributionAnalysis.jsx src/pages/GA4Insights.jsx src/components/SettingsModal.jsx src/pages/MetaAndromedaMonitoring.jsx`
- `npm test -- Analytics`
- `npm run build`

**完成標準**：觸及檔案 lint 無 error；Analytics smoke test 能捕捉未定義變數；build 通過。

#### Phase D：前端二次拆分補強（P2）

**進度**：子項 1 `Analytics.jsx` 已完成（2026-07-14）。抽出 `useAnalyticsObservationImport.js`、`AnalyticsFiltersPanel.jsx`、`AnalyticsKpiSection.jsx`，並額外抽出獨立呈現層 `AnalyticsAiPanel.jsx` 以達到行數目標。`Analytics.jsx` 由 1,848 行降至 946 行；新檔行數分別為 187、533、196、176，皆低於 600 行。驗證：`npm test -- Analytics` 通過、觸及檔案 ESLint 0 error（保留既有 hooks warning）、`npm run build` 通過。

**進度**：子項 2 `ContributionAnalysisComponents.jsx` 已完成（2026-07-14）。原 1,646 行集中檔已改為 13 行 re-export 入口，拆出 `ContributionShared.jsx`、`AccountAndPeriod.jsx`、`AnalysisView.jsx`、`ContributionCharts.jsx`、`ContributionDiagnostics.jsx`、`ContributionAiInsights.jsx`、`GroupEditor.jsx`、`HistoryList.jsx`。新檔行數分別為 324、164、107、389、128、237、248、109，皆低於 600 行。`ContributionAnalysis.jsx` 保持約 674 行頁面組合層，未在本子項擴大重構。驗證：`npm test -- ContributionAnalysis` 通過（16 tests）、觸及檔案 ESLint 0 error、`npm run build` 通過。

**進度**：子項 3 `MonitoringDashboard.jsx` 已完成（2026-07-14）。原 1,804 行集中儀表板降為 69 行組合層；拆出 `MonitoringOverviewPanel.jsx`、`MonitoringEventsPanel.jsx`、`MonitoringDriftReportsPanel.jsx`、`MonitoringScoringProfilesPanel.jsx`、`MonitoringModelSettingsPanel.jsx`、`MonitoringCampaignTrendPanel.jsx` 六個 panel，並額外拆出 `MonitoringHeaderFilters.jsx` 與 `MonitoringDriftDiagnosticsDrawer.jsx`。新檔行數分別為 199、283、186、191、333、344、107、248，皆低於 600 行。驗證：`npm test -- MetaAndromedaMonitoring` 通過（5 tests）、觸及檔案 ESLint 0 error、`npm run build` 通過。

**目標**：處理第十三節發現的「集中搬移到單一大檔」與頁面組合層超標問題，但不一次重拆所有頁面。

**優先順序**：
1. `Analytics.jsx`：最高優先，現況仍約 1,848 行，且已發生 runtime 漏接。先抽 `useAnalyticsObservationImport.js`（Meta Andromeda observation 匯入狀態/handler）、`AnalyticsFiltersPanel.jsx`、`AnalyticsKpiSection.jsx`，目標把頁面降到 <1,000 行，再評估是否追 <400 行。
2. `ContributionAnalysisComponents.jsx`：拆成 `SummaryCards.jsx`、`ContributionTable.jsx`、`FunnelBreakdown.jsx`、`CreativeInsights.jsx` 等子元件；每檔目標 <600 行。
3. `MonitoringDashboard.jsx`：依原第 3 波計劃拆成 6 個 `*Panel.jsx`；每拆一個 panel 後跑 `MetaAndromedaMonitoring.test.jsx`。
4. `GA4Insights.jsx`、`SettingsModal.jsx`：現況功能拆分已較完整，暫列觀察，不主動追 <400 行，除非後續有功能需求順勢拆資料 hook。

**驗收指令**：
- 每完成一頁：對應頁面測試（若有）+ `npx eslint <touched files>` + `npm run build`
- 完成 Phase D 全部後重跑 Phase B 覆核腳本。

**完成標準**：不新增前端 failed tests；所有新元件 <600 行；頁面本體至少較本次覆核現況下降 40%，若仍超過 <400 行，需在文件中註明保留原因。

#### Phase E：重新產出 docs/31（P2）

**狀態**：已完成（2026-07-15 初次產出；2026-07-15 第二輪覆核時補上腳本並修正整體統計口徑）。docs/31 已重新產出，並新增大型檔案拆分狀態表與 Phase D 補強後重點檢查。

**目標**：讓大型檔案統計文件反映第 1-6 波與後續補強後的現況。

**實作任務**：
1. 先執行 Phase A-C，避免 docs/31 記錄到已知 broken 狀態。
2. 重新跑大型檔案統計腳本或等價 `rg --files` + line count 流程，更新 docs/31 的檔案清單、行數、拆分狀態與剩餘候選。
3. 在 docs/31 標記哪些超標是「已接受的組合層例外」、哪些是「待拆候選」。

**統計流程腳本化（第二輪覆核補做）**：初次產出時統計流程只寫成文件描述的「等價流程」，未留下可重跑的腳本，導致第二輪覆核時無法精確複現「411 檔/94,024 行」。已補上 `scripts/validation/source_stats.ps1`，封裝 `git ls-files --cached --others --exclude-standard`，篩選副檔名 `.py .js .jsx .ts .tsx .css .ps1`，排除 `docs/`、`node_modules/`、`dist/`、`.tmp/`、`.vite/`、cache 目錄與 lockfile，依 backend/frontend/root-scripts 分桶輸出檔案數、行數與最大檔案排行，並可用 `-OutFile` 直接輸出可貼回 docs/31 的 Markdown 表格。以此腳本重算得到 **407 個核心原始碼檔、93,959 行**（後端 243/51,923、前端 159/41,263、root/scripts 5/773），docs/31 已改採此數字；與初次產出的 411 檔/94,024 行相比，差異來自初次產出誤將 `README.md`/`GEMINI.md` 等說明文件計入「原始碼」統計，逐檔行數本身兩次一致、無退步。

開發腳本過程中發現並修正一個真實 bug，記錄供之後寫類似統計腳本時參考：`Get-Content file | Measure-Object -Line` 在管線中會靜默少算大型檔案的行數（曾在一個 3,700 行檔案上少算約 300 行）；且 `Get-Content -Raw` 若未明確指定 `-Encoding UTF8`，在部分 PowerShell 版本／環境下對含中文（CJK）內容的 BOM-less UTF-8 檔案解碼會出錯，導致行數統計不穩定（同一檔案在不同呼叫方式下曾算出 714 與 734 兩種結果）。腳本已改用 `-Raw -Encoding UTF8` 讀取後以 `-split "`r?`n"` 計算行數，兩種 PowerShell 呼叫方式（`pwsh`／`powershell.exe`）重跑後結果一致，且與 `wc -l` 逐檔比對相符。

**驗收指令**：
- `powershell -ExecutionPolicy Bypass -File scripts/validation/source_stats.ps1 -Top 40`
- 抽查輸出與 `wc -l <file>` 一致；重新產出的 docs/31 與 `git ls-files`/實際行數抽查一致。
- `git diff -- docs/31* docs/33_大型檔案拆分重構實作計劃.md` 僅包含統計與狀態更新。

**完成標準**：docs/31 最後修改時間晚於第 1-6 波與補強 commit；docs/33 的驗收標準第 6 項可改為完成；統計流程可用單一腳本指令重現，不再依賴無腳本的手動「等價流程」。

#### 第二輪覆核記錄（2026-07-15）

在 Phase A-E 全部標記「已完成」後，針對本節逐項重新實測驗證（而非只讀文件敘述），結果：

| Phase | 驗證方式 | 結果 |
|---|---|---|
| A（真迴歸修復） | 確認 `worker_callbacks.py` 改用 `_shared.SessionLocal()`；跑 `test_scoring_pipeline.py` + `test_meta_andromeda_concurrency.py` | 31 passed，含當初迴歸的測試，屬實 |
| B（覆核腳本） | 實際執行 `refactor_acceptance.ps1 -Backend -SkipExecution -Cleanup` | 腳本可運作、輸出格式與文件描述一致，`-Cleanup` 後 worktree/`git status` 皆乾淨 |
| C（前端 no-undef 防線） | 跑 `Analytics.test.jsx`、對 5 個觸及檔案跑 ESLint | 測試通過；0 errors，僅既有 hooks warning，與文件一致 |
| D（三子項二次拆分） | 實測所有拆出檔案行數、跑對應測試（Analytics/Contribution/Monitoring 共 21 tests）、跑觸及檔案 lint、跑 `npm run build` | 全數通過；行數逐檔吻合文件，僅 `MonitoringCampaignTrendPanel.jsx` 原文件誤植 342，實際 344（已於上方修正） |
| E（docs/31 重新產出） | 抽查表中多筆行數、確認檔案 commit 時間晚於所有 Phase A-D commit；發現整體統計無腳本可重現，因而補做「統計流程腳本化」 | 逐檔行數吻合；整體統計口徑已修正並腳本化（見上） |

**結論**：五個 Phase 皆經得起實測覆核，沒有發現文件造假或退步。本輪覆核新發現並修正兩處文件小瑕疵（`MonitoringCampaignTrendPanel.jsx` 行數誤植、docs/31 整體統計口徑），以及補上可重跑的統計腳本，避免下次覆核再次遇到無法重現的問題。

#### 決策點

- `前端頁面組合層 <400 行` 是否仍作為硬性標準：若是，Phase D 需要繼續抽資料 hooks；若否，建議改成「頁面組合層 <1,000 行且所有葉元件 <600 行」。
- `from ._shared import *` 是否全面替換：若 Phase A 證實問題不限 `SessionLocal`，建議開一個後端 service package hygiene 小波次，集中改成 `_shared.NAME` 存取並跑完整 backend tests。
