# FB 廣告週報功能 — 完整實作規劃方案

> 文件日期：2026-03-02  
> 版本：v1.0  
> 目標：在不改動整體架構的前提下，新增「廣告週報（Weekly Report）」功能，支援條件設定→自動產生→儲存專案→隨時查看。

---

## 一、現況架構盤點

### 1.1 後端（FastAPI）

| 層次 | 路徑 | 說明 |
|------|------|------|
| 入口 | `backend/main.py` | FastAPI App 初始化、Router 掛載 |
| Router | `backend/routers/facebook.py` | FB 廣告 API 端點 |
| Router | `backend/routers/saved_views.py` | 使用者已儲存指標檢視 CRUD |
| 服務層 | `backend/modules/fb_ads/` | `insights_service.py`、`analytics_service.py` 等 |
| 資料庫 | `backend/database/models/` | `user.py`、`team.py`、`view.py` 等 ORM |
| 快取 | `backend/cache.py`、`backend/redis_cache.py` | Insights 快取 |
| DB 遷移 | `backend/alembic/` | Alembic migration |

**現有相關 API（可直接複用）：**
- `GET /api/analytics-data` — 依 since/until 取廣告資料
- `GET /api/analytics-trend` — 取趨勢折線圖資料
- `GET /api/ad-accounts` — 取廣告帳號清單
- AI 摘要串流端點（`aiService.analyzeDataStream`）

---

### 1.2 前端（React + Vite）

| 頁面 | 路由 | 說明 |
|------|------|------|
| `Analytics.jsx` | `/analytics` | FB 廣告手動分析（現有主頁面） |
| `Dashboard.jsx` | `/` | KPI 總覽 |
| `MetricsManager.jsx` | `/metrics` | 指標管理 |

**現有可複用元件：**
- `components/KPICard.jsx` — KPI 指標卡片
- `components/TrendSection.jsx`、`TrendsChart.jsx` — 趨勢折線圖
- `components/Analytics/AnalyticsKPICard.jsx` — 分析版 KPI 卡片
- `components/Analytics/ReportModal.jsx` — 已有的報表預覽彈窗（含 AI 摘要）
- `services/apiClient.js` — 已封裝的 API 基礎請求客戶端

---

## 二、功能需求定義

### 2.1 使用者旅程

```
[建立週報] → 設定條件（帳號 / 日期範圍 / 指標選擇）
           → 儲存草稿
           → 點擊「產生報表」（自動抓取 FB API 資料）
           → 網頁報表渲染（KPI卡片 + 趨勢圖 + 數據表格 + AI摘要）
           → 可編輯章節內容 / 加入備註
           → 儲存完成 → 出現在「報表列表」
[查看列表] → 看到所有週報專案（依日期排序）
           → 點擊進入 → 渲染已儲存的報表
[匯出]     → 列印（使用 window.print()，複用現有邏輯）
```

### 2.2 核心功能項目

| # | 功能 | 優先級 |
|---|------|--------|
| 1 | 報表條件設定精靈（帳號、週次日期、指標） | 🔴 必須 |
| 2 | 自動抓取資料並渲染網頁報表 | 🔴 必須 |
| 3 | 報表專案儲存與列表查看 | 🔴 必須 |
| 4 | AI 摘要自動產生 | 🟡 重要 |
| 5 | 報表章節自訂（標題、備註文字） | 🟡 重要 |
| 6 | 雙週/月報彈性日期支援 | 🟢 選項 |
| 7 | 個人 / 團隊報表隔離 | 🟡 重要 |
| 8 | 列印 / PDF 匯出 | 🟢 選項 |

---

## 三、後端實作方案

### 3.1 新增資料庫模型

**建立 `backend/database/models/report.py`**

```python
# backend/database/models/report.py
"""WeeklyReport ORM 模型 — 週報專案儲存"""

import uuid
from sqlalchemy import Column, String, DateTime, ForeignKey, Text, text

from database.base import Base


class WeeklyReport(Base):
    """
    週報專案。
    記錄使用者設定的條件與產生後的報表資料（JSON快照）。
    """
    __tablename__ = "weekly_reports"

    id           = Column(String, primary_key=True, default=lambda: str(uuid.uuid4()))
    name         = Column(String, nullable=False)           # 報表名稱，e.g. "2026 W09 電商廣告週報"
    description  = Column(Text, nullable=True)              # 自訂備註

    # --- 查詢條件（條件快照） ---
    ad_account_id   = Column(String, nullable=False)        # 廣告帳號 ID
    ad_account_name = Column(String, nullable=True)         # 帳號名稱（快照用）
    date_since      = Column(String, nullable=False)        # YYYY-MM-DD
    date_until      = Column(String, nullable=False)        # YYYY-MM-DD
    date_label      = Column(String, nullable=True)         # e.g. "2026 W09 (2/23~3/1)"
    breakdown       = Column(String, nullable=True)         # 分組依據：campaign / adset / ad / none
    selected_metrics = Column(Text, nullable=False)         # JSON array: ["spend","roas",...]

    # --- 報表資料（產生後快照） ---
    report_data  = Column(Text, nullable=True)              # JSON: { kpis, trend, table_rows, ... }
    ai_summary   = Column(Text, nullable=True)              # AI 摘要文字
    sections     = Column(Text, nullable=True)              # JSON: 自訂章節內容 [{title, content, order}]

    # --- 狀態 ---
    status       = Column(String, default="draft")          # draft | generated | archived

    # --- 擁有者 ---
    user_id      = Column(String, ForeignKey("users.id"), nullable=True, index=True)
    team_id      = Column(String, ForeignKey("teams.id"),  nullable=True, index=True)
    created_by   = Column(String, ForeignKey("users.id"),  nullable=True)

    created_at   = Column(DateTime, default=text("CURRENT_TIMESTAMP"))
    updated_at   = Column(DateTime, default=text("CURRENT_TIMESTAMP"),
                          onupdate=text("CURRENT_TIMESTAMP"))
```

**在 `backend/database/__init__.py` 中匯入新模型（與現有 `SavedView` 同作法）：**

```python
# 在現有 import 後加入
from database.models.report import WeeklyReport
```

---

### 3.2 Alembic 資料庫遷移

**建立 `backend/alembic/versions/xxxx_add_weekly_reports.py`**

```python
"""add weekly_reports table

Revision ID: xxxx_weekly_reports
Revises: <上一個 revision id>
Create Date: 2026-03-02
"""
from alembic import op
import sqlalchemy as sa

def upgrade():
    op.create_table(
        'weekly_reports',
        sa.Column('id',               sa.String(),  primary_key=True),
        sa.Column('name',             sa.String(),  nullable=False),
        sa.Column('description',      sa.Text(),    nullable=True),
        sa.Column('ad_account_id',    sa.String(),  nullable=False),
        sa.Column('ad_account_name',  sa.String(),  nullable=True),
        sa.Column('date_since',       sa.String(),  nullable=False),
        sa.Column('date_until',       sa.String(),  nullable=False),
        sa.Column('date_label',       sa.String(),  nullable=True),
        sa.Column('breakdown',        sa.String(),  nullable=True),
        sa.Column('selected_metrics', sa.Text(),    nullable=False),
        sa.Column('report_data',      sa.Text(),    nullable=True),
        sa.Column('ai_summary',       sa.Text(),    nullable=True),
        sa.Column('sections',         sa.Text(),    nullable=True),
        sa.Column('status',           sa.String(),  default='draft'),
        sa.Column('user_id',          sa.String(),  sa.ForeignKey('users.id'),  nullable=True),
        sa.Column('team_id',          sa.String(),  sa.ForeignKey('teams.id'),  nullable=True),
        sa.Column('created_by',       sa.String(),  sa.ForeignKey('users.id'),  nullable=True),
        sa.Column('created_at',       sa.DateTime(), server_default=sa.text('CURRENT_TIMESTAMP')),
        sa.Column('updated_at',       sa.DateTime(), server_default=sa.text('CURRENT_TIMESTAMP')),
    )
    op.create_index('ix_weekly_reports_user_id', 'weekly_reports', ['user_id'])
    op.create_index('ix_weekly_reports_team_id', 'weekly_reports', ['team_id'])

def downgrade():
    op.drop_index('ix_weekly_reports_team_id',  table_name='weekly_reports')
    op.drop_index('ix_weekly_reports_user_id',  table_name='weekly_reports')
    op.drop_table('weekly_reports')
```

執行指令：
```bash
cd backend
alembic revision --autogenerate -m "add_weekly_reports"
alembic upgrade head
```

---

### 3.3 新增 Router

**建立 `backend/routers/reports.py`**

```python
"""
Weekly Reports Router
週報專案 CRUD + 資料產生端點
"""
from fastapi import APIRouter, Depends, HTTPException
from pydantic import BaseModel
from typing import List, Optional
import json, uuid, logging
from datetime import datetime
from sqlalchemy.orm import Session

from database import SessionLocal, WeeklyReport, User
from dependencies import get_current_user, get_current_team, require_module
from database import Team

logger = logging.getLogger(__name__)

router = APIRouter(prefix="/api/reports", tags=["reports"])

fb_ads_check = require_module("fb_ads")

# ---- Dependency ----
def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()

# ---- Pydantic Schemas ----

class ReportCreate(BaseModel):
    name: str
    description: Optional[str] = None
    ad_account_id: str
    ad_account_name: Optional[str] = None
    date_since: str
    date_until: str
    date_label: Optional[str] = None
    breakdown: Optional[str] = "campaign"
    selected_metrics: List[str]
    team_id: Optional[str] = None

class ReportUpdate(BaseModel):
    name: Optional[str] = None
    description: Optional[str] = None
    sections: Optional[List[dict]] = None
    ai_summary: Optional[str] = None
    status: Optional[str] = None

class ReportResponse(BaseModel):
    id: str
    name: str
    description: Optional[str]
    ad_account_id: str
    ad_account_name: Optional[str]
    date_since: str
    date_until: str
    date_label: Optional[str]
    breakdown: Optional[str]
    selected_metrics: List[str]
    report_data: Optional[dict]
    ai_summary: Optional[str]
    sections: Optional[List[dict]]
    status: str
    user_id: Optional[str]
    team_id: Optional[str]
    created_at: str
    updated_at: Optional[str]

# ---- Helpers ----
def _serialize(report: WeeklyReport) -> dict:
    return {
        "id": report.id,
        "name": report.name,
        "description": report.description,
        "ad_account_id": report.ad_account_id,
        "ad_account_name": report.ad_account_name,
        "date_since": report.date_since,
        "date_until": report.date_until,
        "date_label": report.date_label,
        "breakdown": report.breakdown,
        "selected_metrics": json.loads(report.selected_metrics) if report.selected_metrics else [],
        "report_data": json.loads(report.report_data) if report.report_data else None,
        "ai_summary": report.ai_summary,
        "sections": json.loads(report.sections) if report.sections else [],
        "status": report.status,
        "user_id": report.user_id,
        "team_id": report.team_id,
        "created_at": report.created_at.isoformat() if report.created_at else "",
        "updated_at": report.updated_at.isoformat() if report.updated_at else None,
    }

# ---- Endpoints ----

@router.get("", dependencies=[Depends(fb_ads_check)])
async def list_reports(
    team_id: Optional[str] = None,
    current_user: User = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    """列出個人 + 團隊週報列表"""
    reports = []
    # 個人
    personal = db.query(WeeklyReport)\
        .filter(WeeklyReport.user_id == current_user.id)\
        .order_by(WeeklyReport.created_at.desc()).all()
    reports.extend([_serialize(r) for r in personal])
    # 團隊
    if team_id:
        team_reports = db.query(WeeklyReport)\
            .filter(WeeklyReport.team_id == team_id)\
            .order_by(WeeklyReport.created_at.desc()).all()
        reports.extend([_serialize(r) for r in team_reports])
    return reports


@router.post("", dependencies=[Depends(fb_ads_check)])
async def create_report(
    payload: ReportCreate,
    current_user: User = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    """建立新週報（草稿狀態）"""
    report = WeeklyReport(
        id=str(uuid.uuid4()),
        name=payload.name,
        description=payload.description,
        ad_account_id=payload.ad_account_id,
        ad_account_name=payload.ad_account_name,
        date_since=payload.date_since,
        date_until=payload.date_until,
        date_label=payload.date_label,
        breakdown=payload.breakdown or "campaign",
        selected_metrics=json.dumps(payload.selected_metrics),
        status="draft",
        user_id=current_user.id if not payload.team_id else None,
        team_id=payload.team_id,
        created_by=current_user.id,
        created_at=datetime.utcnow(),
        updated_at=datetime.utcnow(),
    )
    db.add(report)
    db.commit()
    db.refresh(report)
    return _serialize(report)


@router.get("/{report_id}", dependencies=[Depends(fb_ads_check)])
async def get_report(
    report_id: str,
    current_user: User = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    """取得單一週報"""
    report = db.query(WeeklyReport).filter(WeeklyReport.id == report_id).first()
    if not report:
        raise HTTPException(status_code=404, detail="Report not found")
    return _serialize(report)


@router.put("/{report_id}", dependencies=[Depends(fb_ads_check)])
async def update_report(
    report_id: str,
    payload: ReportUpdate,
    current_user: User = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    """更新週報（名稱、章節、AI摘要、狀態）"""
    report = db.query(WeeklyReport).filter(WeeklyReport.id == report_id).first()
    if not report:
        raise HTTPException(status_code=404, detail="Report not found")
    if payload.name is not None:
        report.name = payload.name
    if payload.description is not None:
        report.description = payload.description
    if payload.sections is not None:
        report.sections = json.dumps(payload.sections)
    if payload.ai_summary is not None:
        report.ai_summary = payload.ai_summary
    if payload.status is not None:
        report.status = payload.status
    report.updated_at = datetime.utcnow()
    db.commit()
    return _serialize(report)


@router.delete("/{report_id}", dependencies=[Depends(fb_ads_check)])
async def delete_report(
    report_id: str,
    current_user: User = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    """刪除週報"""
    report = db.query(WeeklyReport).filter(WeeklyReport.id == report_id).first()
    if not report:
        raise HTTPException(status_code=404, detail="Report not found")
    db.delete(report)
    db.commit()
    return {"message": "deleted"}


@router.post("/{report_id}/generate", dependencies=[Depends(fb_ads_check)])
async def generate_report(
    report_id: str,
    current_user: User = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    """
    觸發報表資料產生：
    複用現有 analytics_service 抓取 FB 廣告資料，
    快照儲存至 report_data 欄位，狀態改為 generated。
    """
    report = db.query(WeeklyReport).filter(WeeklyReport.id == report_id).first()
    if not report:
        raise HTTPException(status_code=404, detail="Report not found")

    # 呼叫現有 analytics service（直接複用）
    from modules.fb_ads.analytics_service import get_analytics_data
    metrics = json.loads(report.selected_metrics)
    data = await get_analytics_data(
        account_id=report.ad_account_id,
        since=report.date_since,
        until=report.date_until,
        user_id=current_user.google_id,   # google_id for FB token lookup
        breakdown=report.breakdown or "campaign",
    )

    if data is None:
        raise HTTPException(status_code=502, detail="Failed to fetch FB data")

    # 快照儲存
    report.report_data = json.dumps(data)
    report.status = "generated"
    report.updated_at = datetime.utcnow()
    db.commit()

    return _serialize(report)
```

**在 `backend/main.py` 掛載（僅新增兩行）：**

```python
# 於現有 router include 段落新增
from routers.reports import router as reports_router
app.include_router(reports_router)
```

---

## 四、前端實作方案

### 4.1 目錄結構規劃（新增部分）

```
frontend/src/
├── pages/
│   ├── Reports.jsx              ← NEW：週報專案列表頁
│   └── ReportViewer.jsx         ← NEW：報表閱覽/編輯頁
├── components/
│   └── Reports/                 ← NEW：週報相關元件
│       ├── index.js
│       ├── ReportCard.jsx       ← 列表卡片
│       ├── ReportConfig.jsx     ← 條件設定面板（精靈）
│       ├── ReportKPISection.jsx ← KPI 卡片區（複用 KPICard）
│       ├── ReportTrendSection.jsx ← 趨勢圖區（複用 TrendsChart）
│       ├── ReportTableSection.jsx ← 數據明細表格
│       ├── ReportNoteEditor.jsx ← 備註/章節文字編輯
│       └── WeeklyReportTemplate.jsx ← 週報完整模板（組合以上）
├── services/
│   └── reportService.js         ← NEW：報表 API 呼叫層
└── hooks/
    └── useReports.js            ← NEW：報表資料管理 hook
```

---

### 4.2 reportService.js

```javascript
// frontend/src/services/reportService.js
import apiClient from './apiClient';

export const reportService = {
  /** 取得報表列表 */
  list: (teamId) =>
    apiClient.get('/api/reports', { params: teamId ? { team_id: teamId } : {} }),

  /** 取得單筆報表 */
  get: (id) => apiClient.get(`/api/reports/${id}`),

  /** 建立新報表（草稿） */
  create: (payload) => apiClient.post('/api/reports', payload),

  /** 更新報表（名稱/章節/AI摘要） */
  update: (id, payload) => apiClient.put(`/api/reports/${id}`, payload),

  /** 刪除報表 */
  delete: (id) => apiClient.delete(`/api/reports/${id}`),

  /** 觸發資料產生 */
  generate: (id) => apiClient.post(`/api/reports/${id}/generate`),
};
```

---

### 4.3 Reports.jsx（列表頁）功能說明

**路由：`/reports`**

| 區塊 | 說明 |
|------|------|
| 頁首 | 標題「週報管理」+ 「+ 建立週報」按鈕 |
| 搜尋/篩選列 | 依狀態篩選（草稿 / 已產生 / 全部） |
| 卡片格 | 每個報表顯示 `ReportCard`（名稱、日期範圍、帳號、狀態 badge） |
| 卡片操作 | 「查看」→ 跳轉 `/reports/:id`；「刪除」→ 確認後刪除 |
| 空狀態 | 無報表時顯示引導提示 + 建立按鈕 |

---

### 4.4 ReportViewer.jsx（報表閱覽/建立頁）功能說明

**路由：`/reports/new`（建立）、`/reports/:id`（查看/編輯）**

#### 精靈化建立流程（StepWizard）

```
Step 1: 基本設定
  - 報表名稱、說明
  - 廣告帳號選擇（複用現有 AdAccountSelector）
  - 個人 or 團隊

Step 2: 日期設定
  - 快速選項：上週 / 本週 / 上個月（預設週一到週日）
  - 自訂日期範圍
  - 自動計算「W週次」標籤，e.g. "2026 W09 (2/23~3/1)"

Step 3: 指標選擇
  - 複用現有 MetricSelector 元件
  - 預設套用「總覽」preset（spend, roas, ctr, cpc, purchases...）

Step 4: 產生預覽
  - 「儲存草稿」按鈕 → 儲存後跳轉 /reports/:id
  - 「立即產生報表」→ POST /api/reports + POST /api/reports/:id/generate
```

#### 報表閱覽視圖（`/reports/:id`）

```
[已產生狀態]

┌─────────────────────────────────────────────────────────────┐
│  📊 2026 W09 電商廣告週報 · act_xxx · 2/23 ~ 3/1            │
│  [重新產生]  [產生 AI 摘要]  [列印/匯出]  [編輯]            │
├─────────────────────────────────────────────────────────────┤
│  SECTION 01 — 本週關鍵指標 (KPI Overview)                   │
│  ┌──────┐┌──────┐┌──────┐┌──────┐┌──────┐┌──────┐         │
│  │Spend ││ROAS  ││CTR   ││CPC   ││購買  ││曝光  │         │
│  └──────┘└──────┘└──────┘└──────┘└──────┘└──────┘         │
├─────────────────────────────────────────────────────────────┤
│  SECTION 02 — 趨勢走勢 (7日折線圖，複用 TrendsChart)        │
│  [折線圖區域]                                                │
├─────────────────────────────────────────────────────────────┤
│  SECTION 03 — 廣告活動明細 (Campaign Table)                 │
│  [可排序資料表：依廣告層級展開]                              │
├─────────────────────────────────────────────────────────────┤
│  SECTION 04 — AI 成效分析摘要                               │
│  [AI Markdown 摘要文字，複用 ReportModal 的 aiService 邏輯] │
├─────────────────────────────────────────────────────────────┤
│  SECTION 05 — 本週備註 & 後續建議（可編輯文字區塊）          │
│  [自訂 textarea，儲存至 sections JSON]                       │
└─────────────────────────────────────────────────────────────┘
```

---

### 4.5 WeeklyReportTemplate.jsx（報表模板元件）

週報視圖由以下子元件組合：

```jsx
<WeeklyReportTemplate reportData={report}>
  <ReportHeader />              {/* 報表名稱、日期、帳號、操作按鈕 */}
  <ReportKPISection />          {/* 複用 KPICard，整排展示選定指標 */}
  <ReportTrendSection />        {/* 複用 TrendsChart + TrendSection */}
  <ReportTableSection />        {/* 廣告層級明細表格，可展開 */}
  <ReportAISummary />           {/* 複用 aiService，串流顯示 Markdown */}
  <ReportNoteEditor />          {/* 備註自訂章節，autosave */}
</WeeklyReportTemplate>
```

---

### 4.6 路由與導覽設定

**`frontend/src/App.jsx` 新增路由：**

```jsx
// 在現有 lazy imports 後新增
const Reports     = lazy(() => import('./pages/Reports'));
const ReportViewer = lazy(() => import('./pages/ReportViewer'));

// 在 <Routes> 內 ProtectedRoute > Layout 下新增
<Route path="/reports" element={
  <ProtectedModule module="fb_ads">
    <ErrorBoundary>
      <Reports />
    </ErrorBoundary>
  </ProtectedModule>
} />
<Route path="/reports/new" element={
  <ProtectedModule module="fb_ads">
    <ErrorBoundary>
      <ReportViewer mode="create" />
    </ErrorBoundary>
  </ProtectedModule>
} />
<Route path="/reports/:id" element={
  <ProtectedModule module="fb_ads">
    <ErrorBoundary>
      <ReportViewer mode="view" />
    </ErrorBoundary>
  </ProtectedModule>
} />
```

**`frontend/src/components/Sidebar.jsx` 新增選單項目：**

```jsx
// 在 'Analytics'（成效分析）之後新增
{ 
  icon: <FiFileText size={20} />, 
  label: t('Weekly Reports', '週報管理'), 
  path: '/reports' 
},
```

（需在 Sidebar.jsx 上方引入 `FiFileText`，該圖示已在 `react-icons/fi` 中存在）

---

## 五、資料流設計

### 5.1 建立 + 產生報表流程

```
前端 ReportViewer (Step 4)
  │
  ├─ POST /api/reports  ─────────────────────────────→ DB 寫入草稿
  │    payload: { name, ad_account_id, since, until,      ↓
  │               selected_metrics, breakdown }       WeeklyReport
  │                                                  (status=draft)
  ├─ POST /api/reports/:id/generate
  │    ↓
  │  後端呼叫 modules/fb_ads/analytics_service.get_analytics_data()
  │    ↓（現有服務，直接複用）
  │  FB Graph API 回傳資料
  │    ↓
  │  report.report_data = JSON.stringify(data)
  │  report.status = "generated"
  │    ↓
  │  回傳完整 ReportResponse
  │
  └─ 前端收到 → 渲染 WeeklyReportTemplate
```

### 5.2 AI 摘要流程

```
前端點擊「產生 AI 摘要」
  │
  ├─ 呼叫現有 aiService.analyzeDataStream(
  │     reportData.kpis,
  │     context = "週報摘要,日期範圍:...,帳號:...",
  │     type = "weekly_summary"
  │  )
  │    ↓（串流）
  │  逐字顯示 Markdown 摘要
  │
  └─ 完成後 PUT /api/reports/:id { ai_summary: "..." }
       ↓
     儲存至 DB
```

---

## 六、UI 設計規範

### 6.1 週報列表頁（Reports.jsx）

```
┌─────────────────────────────────────────────┐
│  📋 週報管理                    [+ 建立週報] │
│  ─────────────────────────────────────────   │
│  [全部 ▼]  [草稿]  [已完成]  搜尋...        │
│                                               │
│  ┌──────────────┐  ┌──────────────┐          │
│  │ 2026 W09      │  │ 2026 W08      │          │
│  │ 電商週報      │  │ 品牌週報      │          │
│  │ act_12345     │  │ act_12345     │          │
│  │ 2/23 ~ 3/1    │  │ 2/16 ~ 2/22   │          │
│  │ ● 已產生      │  │ ○ 草稿        │          │
│  │ [查看] [刪除] │  │ [查看] [刪除] │          │
│  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────┘
```

### 6.2 報表視圖（ReportViewer.jsx）

- 採用與現有 Analytics 頁面相同的深色主題
- KPI 卡片複用 `AnalyticsKPICard`，保持視覺一致
- 趨勢圖複用 `TrendsChart`，同樣的 Recharts 折線圖
- 表格與 Analytics.jsx 的 Campaign Table 相同樣式
- AI 摘要區使用 `ReactMarkdown` 渲染，與 `ReportModal` 一致
- 匯出採用 `window.print()` + `@media print` CSS，沿用現有做法

### 6.3 報表狀態 Badge 顏色定義

| 狀態 | 標籤 | 顏色 |
|------|------|------|
| `draft` | 草稿 | `var(--text-secondary)` 灰色 |
| `generated` | 已產生 | `#10b981` 綠色 |
| `archived` | 已封存 | `#6b7280` 暗灰 |

---

## 七、不影響現有架構的說明

| 層次 | 現有 | 新增策略 |
|------|------|---------|
| 資料庫 | `saved_views`, `users`, `teams` 等 | 新增獨立 `weekly_reports` 表，零影響 |
| Backend Router | `facebook.py`, `saved_views.py` 等 | 新增獨立 `reports.py`，在 `main.py` 附加掛載 |
| 業務服務 | `analytics_service.py` 等 | 直接 import 呼叫，不修改原服務 |
| 前端路由 | `/`, `/analytics`, `/metrics` 等 | 新增 `/reports/*`，不修改現有路由 |
| 前端元件 | `KPICard`, `TrendsChart` 等 | 作為 props 傳入複用，不修改原始元件 |
| Sidebar | 現有選單項目 | 僅新增一個選單項目，不調整順序 |
| API Client | `apiClient.js` | 透過新 `reportService.js` 呼叫，不修改原 service |

---

## 八、分階段實作建議

### Phase 1 — 核心骨架（約 2-3 天）

- [ ] 建立 `WeeklyReport` 模型 + Alembic migration
- [ ] 實作 `routers/reports.py`（CRUD + generate 端點）
- [ ] 在 `main.py` 掛載 Router
- [ ] 建立 `reportService.js`
- [ ] 建立 `Reports.jsx` 列表頁（CRUD UI）
- [ ] App.jsx 新增路由 + Sidebar 新增項目

### Phase 2 — 報表閱覽 UI（約 3-4 天）

- [ ] 建立 `ReportViewer.jsx` 精靈式建立流程（Step 1~4）
- [ ] 建立 `WeeklyReportTemplate.jsx` 週報模板
- [ ] 建立 `ReportKPISection`、`ReportTrendSection`、`ReportTableSection`
- [ ] 整合現有 `AdAccountSelector`、`MetricSelector` 至精靈

### Phase 3 — AI 摘要 + 備註編輯（約 1-2 天）

- [ ] 整合 `aiService.analyzeDataStream` 至 `ReportAISummary`
- [ ] 建立 `ReportNoteEditor`（自訂章節文字，autosave PUT）
- [ ] AI 摘要完成後自動儲存至後端

### Phase 4 — 優化與列印（約 1 天）

- [ ] `@media print` CSS 調整報表列印樣式
- [ ] 週次標籤自動計算（`date-fns` 的 `getWeek`）
- [ ] 無資料 / 錯誤狀態處理

---

## 九、涉及檔案清單彙總

### 新增檔案

| 類型 | 路徑 |
|------|------|
| 後端模型 | `backend/database/models/report.py` |
| 後端遷移 | `backend/alembic/versions/xxxx_add_weekly_reports.py` |
| 後端 Router | `backend/routers/reports.py` |
| 前端 Service | `frontend/src/services/reportService.js` |
| 前端 Hook | `frontend/src/hooks/useReports.js` |
| 前端頁面 | `frontend/src/pages/Reports.jsx` |
| 前端頁面 | `frontend/src/pages/ReportViewer.jsx` |
| 前端元件 | `frontend/src/components/Reports/index.js` |
| 前端元件 | `frontend/src/components/Reports/ReportCard.jsx` |
| 前端元件 | `frontend/src/components/Reports/ReportConfig.jsx` |
| 前端元件 | `frontend/src/components/Reports/WeeklyReportTemplate.jsx` |
| 前端元件 | `frontend/src/components/Reports/ReportKPISection.jsx` |
| 前端元件 | `frontend/src/components/Reports/ReportTrendSection.jsx` |
| 前端元件 | `frontend/src/components/Reports/ReportTableSection.jsx` |
| 前端元件 | `frontend/src/components/Reports/ReportNoteEditor.jsx` |

### 修改檔案（最小範圍）

| 類型 | 路徑 | 修改內容 |
|------|------|---------|
| 後端入口 | `backend/main.py` | 新增 2 行：import + include_router |
| 後端資料庫 | `backend/database/__init__.py` | 新增 1 行：import WeeklyReport |
| 前端路由 | `frontend/src/App.jsx` | 新增約 20 行（lazy import + 3 Route） |
| 前端導覽 | `frontend/src/components/Sidebar.jsx` | 新增 1 個 menuItems 項目 |

---

## 十、後續擴充可能性

| 方向 | 說明 |
|------|------|
| 定時自動週報 | 後端新增 Celery/APScheduler 週期任務，自動 generate |
| 報表分享連結 | 新增 `share_token` 欄位，產生公開唯讀 URL |
| 多帳號合併週報 | 支援多個 ad_account_id，合計匯總 |
| 週報範本市集 | 預設範本（電商版、品牌版、代理商版）快速套用 |
| Email 寄送 | 週報產生後觸發 Email 通知（需整合 SMTP / Resend） |

---

*本文件依現有 DataVue-App 專案架構撰寫，所有新增功能均採「垂直疊加」模式，不影響 Analytics、Dashboard 等現有頁面的運作。*
