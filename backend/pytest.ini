[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    unit: 單元測試（不需要 DB 連線）
    integration: 整合測試（需要 DB 連線）
    slow: 執行時間較長的測試
asyncio_mode = auto
